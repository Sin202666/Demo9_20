# Repo Git defaults. Applied after running scripts/ugit/1-安装或更新打表钩子.bat
# (.git/config: include.path = ../.gitconfig)
#
# alias.ugit-sync + git.path wrapper (UGit-Git.cmd) are configured by Install-GitHook.ps1
[pull]
	rebase = true
[rebase]
	autoStash = true
