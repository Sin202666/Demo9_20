# 模块规范性审查名单

按 [`代码结构和设计开发规范.md`](代码结构和设计开发规范.md) / [`代码注释和说明开发规范.md`](代码注释和说明开发规范.md) 做 **模块规范性审查** 的进度总表。  
审查技能：`.cursor/skills/module-standard-review/`。

> 创建：2026-08-28 · 作者：林奥宇 · Last Modified: 2026-09-01（Feedback 复审并修 P0/P1/P2，改为通过）  
> 口径：历史对话里出过「模块规范性审查报告」的算已审；方案二次审评 / 手测封板 / [上线前审查](上线前审查标准.md) **不等于** 本表审查。  
> 范围：System / SystemModule / ToolSystem / ToolBasic / Manager / GuiScripts / Class。第三方 `SmartBone` 不纳入。

---

## 0. 怎么用

1. **一次只开一批。** 对 Agent 说：`按审查名单做 B1`（或 `B1-1`）。  
2. 审查默认 **只读出报告、不改代码**；要修再说「按报告修 P0」。  
3. 一批审完，把本表对应行的状态改成：通过 / 有条件通过 / 不通过，并填日期。  
4. 随父模块一起审过的，标 **随审**，不再单开。  
5. 已审但结论为不通过 / 有条件通过的，见 [§4 复审队列](#4-已审查归档复审队列)，不与未审批次抢位。

| 状态 | 含义 |
|------|------|
| 未审 | 从未出过规范性审查报告 |
| 审查中 | 本批已开工、报告未写回 |
| 通过 / 有条件通过 / 不通过 | 最近一次报告结论 |
| 随审 | 已作为其它模块报告的一部分覆盖 |
| 跳过 | 明确不审（第三方、废弃、测试闸） |

---

## 1. 批次总览

未审顶层模块 **0** 个。拆成 **7 批**，已全部出过报告。优先权威写口、运动、输入，再基础设施与 UI。

| 批 | 主题 | 模块数 | 建议顺序 | 状态 |
|----|------|--------|----------|------|
| **B1** | 逻辑怪运动与调度 | 16 | 1 | B1 已审完 |
| **B2** | 输入 / 坐骑 / 锁 | 12 | 2 | B2 已审完 |
| **B3** | 战斗、角色运动、技能接线 | 18 | 3 | B3 已审完 |
| **B4** | 存档、CDK、排行、全局态 | 10 | 4 | B4 已审完 |
| **B5** | ToolBasic 基础设施 | 15 | 5 | B5 已审完 |
| **B6** | 剩余 Manager | 10 | 6 | B6 已审完 |
| **B7** | GuiScripts / Class / 杂项 | 12 | 7 | B7 已审完 |

B1 体量大，可拆两刀：`B1-1` 运动核 → `B1-2` 入口与 Store。

---

## 2. 未审查明细

勾选表示「本批已出报告」。结论列填通过 / 有条件通过 / 不通过。

### B1 逻辑怪运动与调度

对照计划：[`逻辑怪位移与寻路开发计划.md`](逻辑怪位移与寻路开发计划.md)。M0–M2 已手测。**B1-1 / B1-2 均已出报告。** 重点：§1.6 / §1.7 写口、无 `IsServer` 混写。

#### B1-1 运动核

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | LogicEntityMotion | `src/ReplicatedFirst/AllSideCode/ToolSystem/LogicEntityMotion.luau` | 通过（复审） | 2026-09-01 |
| [x] | LogicalMoveCommand | `src/ReplicatedFirst/AllSideCode/ToolSystem/LogicalMoveCommand.luau` | 通过 | 2026-08-28 |
| [x] | LogicalPathFollow | `src/ReplicatedFirst/AllSideCode/ToolSystem/LogicalPathFollow.luau` | 通过（复审） | 2026-09-01 |
| [x] | PathFindUtil | `src/ReplicatedFirst/AllSideCode/ToolSystem/PathFindUtil.luau` | 通过 | 2026-08-28 |
| [x] | EnemySim | `src/ReplicatedFirst/AllSideCode/ToolSystem/EnemySim.luau` | 通过（复审） | 2026-08-28 |

#### B1-2 入口 / Store / 编排

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | RangerAi | `src/ReplicatedFirst/AllSideCode/ToolSystem/RangerAi.luau` | 通过（复审） | 2026-08-28 |
| [x] | SystemEnemy（服） | `src/ServerStorage/ServerSideCode/System/SystemEnemy.luau` | 通过（复审） | 2026-08-28 |
| [x] | SystemEnemy（AllSide stub） | `src/ReplicatedFirst/AllSideCode/ToolSystem/SystemEnemy.luau` | 通过（复审） | 2026-08-28 |
| [x] | SystemLogicalEnemy | `src/ReplicatedStorage/ClientSideCode/SystemModule/SystemLogicalEnemy.luau` | 通过（复审） | 2026-09-01 |
| [x] | EnemyLogicalStore | `src/ReplicatedFirst/AllSideCode/ToolSystem/EnemyLogicalStore.luau` | 通过（复审） | 2026-09-01 |
| [x] | EnemyLogicalSync | `src/ReplicatedFirst/AllSideCode/ToolSystem/EnemyLogicalSync.luau` | 通过（复审） | 2026-08-28 |
| [x] | EnemyLogicalTypes | `src/ReplicatedFirst/AllSideCode/ToolSystem/EnemyLogicalTypes.luau` | 通过（复审） | 2026-08-28 |
| [x] | LogicalAirborne | `src/ReplicatedFirst/AllSideCode/ToolSystem/LogicalAirborne.luau` | 通过（复审） | 2026-08-28 |
| [x] | LogicalAirborneProfile | `src/ReplicatedFirst/AllSideCode/ToolSystem/LogicalAirborneProfile.luau` | 通过（复审） | 2026-08-28 |
| [x] | EnemyManager | `src/StarterPlayer/StarterPlayerScripts/Manager/EnemyManager.client.luau` | 通过 | 2026-08-28 |
| [x] | LogicalEnemySpawnTest | `src/ServerScriptService/InitGame/Manager/LogicalEnemySpawnTest.server.luau` | 跳过 | 2026-08-28 |

`LogicalDisplace` / `LogicalDisplaceProfile` 初审不通过（2026-08-17）；2026-09-01 复审并修后改为通过。

B1-2 P0 已修（2026-08-28）：Ranger 朝向改 `_desiredLook`（Motion 消费）；`SystemEnemy.Init` 不再互调 `EnemySim.Init`；AllSide stub 与 Types/Sync/Airborne 注释已补。**复审见下表结论。**

---

### B2 输入 / 坐骑 / 锁

对照：[`SystemInput模块设计计划.md`](SystemInput模块设计计划.md)、[`坐骑系统开发说明.md`](坐骑系统开发说明.md)。**B2 均已出报告。**

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | SystemInput | `src/ReplicatedStorage/ClientSideCode/SystemModule/SystemInput/` | 通过（复审） | 2026-09-01 |
| [x] | SystemInputBind | `src/ServerStorage/ServerSideCode/System/SystemInputBind.luau` | 通过 | 2026-08-28 |
| [x] | PlayerInputClientManager | `src/StarterPlayer/StarterPlayerScripts/Manager/PlayerInputClientManager.client.luau` | 通过 | 2026-08-28 |
| [x] | ActionArbiter | `src/ReplicatedFirst/AllSideCode/ToolSystem/ActionArbiter/` | 通过（复审） | 2026-09-01 |
| [x] | SystemMount（客） | `src/ReplicatedStorage/ClientSideCode/SystemModule/SystemMount/` | 通过（复审） | 2026-09-01 |
| [x] | SystemMount（服） | `src/ServerStorage/ServerSideCode/System/SystemMount.luau` | 通过（复审） | 2026-09-01 |
| [x] | MountNet | `src/ReplicatedFirst/AllSideCode/ToolSystem/MountNet.luau` | 通过（复审） | 2026-08-28 |
| [x] | LocoStyle | `src/ReplicatedFirst/AllSideCode/ToolSystem/LocoStyle/` | 通过（复审） | 2026-09-01 |
| [x] | LocoStyleSets | `src/ReplicatedFirst/AllSideCode/ToolSystem/LocoStyleSets.luau` | 通过 | 2026-08-28 |
| [x] | PlayerMountClientManager | `src/StarterPlayer/StarterPlayerScripts/Manager/PlayerMountClientManager.client.luau` | 通过 | 2026-08-28 |
| [x] | PlayerMountManager | `src/ServerScriptService/InitGame/Manager/PlayerMountManager.server.luau` | 通过 | 2026-08-28 |
| [x] | CameraImpulse | `src/ReplicatedFirst/AllSideCode/ToolSystem/CameraImpulse.luau` | 通过（复审） | 2026-08-28 |

B2 注释 P0 已闭合（2026-08-28 复审）。无 §1.6/§1.7 红牌。B2 P1 已修（注释/Utils/freeze/Init 接线/限频/Died 连接）。2026-09-01：客 RequestExit 注释 + `destroy`/`Unbind`、服 TryExit `reason`、Presentation 走 Utils；SystemInput 查询拷贝；ActionArbiter `Create` 拆 `GateInstance`；LocoStyle `Create` 拆 `StyleInstance`。未改存档拥有、未把 Jump 收敛到 SystemInput。

---

### B3 战斗、角色运动、技能接线

`SystemSkill` **服装备权威**已复审通过。`BaseSkill` / `GroupSkill` 运行时拆层方案（[`BaseSkillGroupSkill拆层方案.md`](BaseSkillGroupSkill拆层方案.md)）§1.8 **有条件通过**后已落地（2026-08-31）。**2026-09-01 复审+修注释 P0/黄项：** 架构 / `IsServer` / 私连 Remote / §1.6 / §1.7 已闭合；注释格式、`_` 前缀、`Log`、`Tick`/`applyHit` 拆段、`HitResolver` 拆子文件、客瞄准 Heartbeat 离场 Disconnect 已补。改为**通过**（🟡=0）。本批只收 **未单独出报告** 的战斗/运动邻接。

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | BaseSkill / GroupSkill 运行时 | `SystemSkillRuntime/` · `HitResolver/` · `PlayerAimSync` | 通过（复审） | 2026-09-01 |
| [x] | CombatWorld | `src/ReplicatedFirst/AllSideCode/ToolSystem/CombatWorld.luau` | 通过（复审） | 2026-08-28 |
| [x] | LogicalHitValidate | `src/ReplicatedFirst/AllSideCode/ToolSystem/LogicalHitValidate.luau` | 通过 | 2026-08-28 |
| [x] | SystemCharacterMotion | `src/ServerStorage/ServerSideCode/System/SystemCharacterMotion.luau` | 通过（复审） | 2026-09-01 |
| [x] | SystemPlrAttr（服） | `src/ServerStorage/ServerSideCode/System/SystemPlrAttr.luau` | 通过（复审） | 2026-08-28 |
| [x] | SystemPlrAttr（AllSide） | `src/ReplicatedFirst/AllSideCode/ToolSystem/SystemPlrAttr.luau` | 通过（复审） | 2026-08-28 |
| [x] | SkillNet | `src/ReplicatedFirst/AllSideCode/ToolSystem/SkillNet.luau` | 通过（复审） | 2026-08-28 |
| [x] | SkillBuffUtil（服） | `src/ServerStorage/ServerSideCode/System/SkillBuffUtil.luau` | 通过（复审） | 2026-08-31 |
| [x] | SkillBuffUtil（AllSide stub） | `src/ReplicatedFirst/AllSideCode/ToolSystem/SkillBuffUtil.luau` | 通过（复审） | 2026-08-31 |
| [x] | SkillHitPresentation（服） | `src/ServerStorage/ServerSideCode/System/SkillHitPresentation.luau` | 通过（复审） | 2026-09-01 |
| [x] | SkillHitPresentation（客 stub） | `src/ReplicatedFirst/AllSideCode/ToolSystem/SkillHitPresentation/` | 通过（复审） | 2026-09-01 |
| [x] | NPCCosmeticHit（服） | `src/ServerStorage/ServerSideCode/System/NPCCosmeticHit.luau` | 通过（复审） | 2026-08-31 |
| [x] | NPCCosmeticHit（客 stub） | `src/ReplicatedFirst/AllSideCode/ToolSystem/NPCCosmeticHit/` | 通过（复审） | 2026-08-31 |
| [x] | SystemSummon | `src/ReplicatedFirst/AllSideCode/ToolSystem/SystemSummon.luau` | 通过（复审） | 2026-08-28 |
| [x] | SystemPlayerSkill（服） | `src/ServerStorage/ServerSideCode/System/SystemPlayerSkill.luau` | 通过（复审） | 2026-08-31 |
| [x] | SystemPlayerSkill（客） | `src/ReplicatedStorage/ClientSideCode/SystemModule/SystemPlayerSkill.luau` | 通过（复审） | 2026-08-31 |
| [x] | PlayerSkillClientManager | `src/StarterPlayer/StarterPlayerScripts/Manager/PlayerSkillClientManager.client.luau` | 通过（复审） | 2026-08-31 |
| [x] | PlayerSkillManager | `src/ServerScriptService/InitGame/Manager/PlayerSkillManager.server.luau` | 通过（复审） | 2026-08-31 |
| [x] | SkillDemoCast | `src/ReplicatedStorage/ClientSideCode/SystemSkill/SkillModule/SkillDemoCast.luau` | 通过 | 2026-08-28 |
| [x] | SkillDemoProjectile | `src/ReplicatedStorage/ClientSideCode/SystemSkill/SkillModule/SkillDemoProjectile.luau` | 通过（复审） | 2026-08-28 |
| [x] | SpaceSkill1 | `src/ReplicatedStorage/ClientSideCode/SystemSkill/SkillModule/SpaceSkill1.luau` | 通过（复审） | 2026-08-28 |
| [x] | SkillCommon / Projectile 模板 | `.../SkillModule/_Templates/` | 通过（复审） | 2026-08-28 |
| [x] | AnimationLayer | `src/ReplicatedFirst/AllSideCode/ToolSystem/AnimationLayer/` | 通过（复审） | 2026-08-28 |
| [x] | SpeedLineFX | `src/ReplicatedFirst/AllSideCode/ToolSystem/SpeedLineFX.luau` | 通过 | 2026-08-28 |

`NewRoll`、`Meteor1` 已审，不进本批。`CharacterMotion` / `CharacterControl` 已审。

B3 无 §1.6/§1.7 红牌。**B3 P0 复审（2026-08-28）：** 注释/Utils/`DASH_SLOT_INDEX` 已闭合。仍不通过（架构，未拆）：NPCCosmeticHit（`IsServer` 内联服逻辑）、PlayerSkillClientManager / PlayerSkillManager（编排过厚）。有条件通过：SystemCharacterMotion、SkillHitPresentation（黄项＞3）。其余注释红已改通过。

**SystemCharacterMotion 复审（2026-09-01）：** Utils/Log/Init/destroy/无窗断 Heartbeat/魔法数常量/注释字段形已闭合。改为**通过**（🟡=0）。§1.8 残留：既有包络规范修复，无新 Remote/存档/写口；能力默认 tween 仍 0.7（未改成 `DEFAULT_TWEEN_DURATION` 0.4）。

**SkillHitPresentation 复审（2026-09-01）：** 机械黄闭合（表名 / 公共注释 / Config·SoundMap freeze / `Log.warn` / 常量 / Utils.Players）。`init.luau` 仍 `RunService:IsServer()` 混写，对照 NPCCosmeticHit 拆层须 §1.8。改为**不通过**。

**SkillHitPresentation 拆层方案（2026-09-01）：** 见 [`SkillHitPresentation拆层方案.md`](SkillHitPresentation拆层方案.md)。§1.8 二次审评 **有条件通过** 后已开工落地。

**SkillHitPresentation 拆层复审（2026-09-01）：** 架构 / `IsServer` / §1.6 / §1.7 已闭合。服 System 写口 + 客 ToolSystem stub，对照 NPCCosmeticHit。黄项已修（SoundMap 头、`logicalId`）。改为**通过**（服/客 🟡=0）。§1.8 残留：服经 `Utils.ReplicatedFirst` 容器 require Profile/SoundMap（方案 §7.6，不扩 Utils 键）。

**B3 不通过三项复审（2026-08-31）：** 架构 P0 未动。NPCCosmeticHit 仍 `RunService:IsServer()` 混写 Nearby 下行；两份 Skill Manager 仍含跨槽裁决 / 旁观工厂 / 释放校验。对照 SkillBuffUtil 拆写口，本三项拆分须走 §1.8。维持**不通过**。

**B3 三项拆层方案（2026-08-31）：** 见 [`B3三项拆层方案.md`](B3三项拆层方案.md)。§1.8 二次审评 **有条件通过** 后已开工落地。

**B3 拆层复审（2026-08-31）：** 架构 / `IsServer` / Manager 过厚 / §1.6 / §1.7 均闭合。改为通过：NPCCosmeticHit（服/客）、PlayerSkillManager、PlayerSkillClientManager。新建 SystemPlayerSkill 服/客初审为有条件通过（黄项＞3）。B3 本批无残留不通过。

**SystemPlayerSkill P1（2026-08-31）：** 离场先 destroy 各槽再清表；抽出 `_onUnbind` / `_watchSlot` / `_findFromRuntime`；Init 挪到 return 前；OnRelease `@param releaseData` 补字段形；旁观 destroy 失败 `Log.warn`；槽位 Folder 名进 `EnumMgr.SkillFolder`。长路径 require 按方案保留。改为**通过**（服/客 🟡≤3，无 🔴）。

**SkillBuffUtil 拆写口（2026-08-31，§1.8 有条件通过后开工）：** 写口迁 `ServerSideCode/System`，无 `IsServer` 业务分支；Init 只 register，不互调 `CombatWorld.Init`；ToolSystem 仅查询 stub；Utils 按 `SystemEnemy` 分流。调用方未改。

**SkillBuffUtil 复审（2026-08-31）：** 架构 / §1.6 / §1.7 / `IsServer` 均闭合。P1 已修：`destroy` + Tick 缓存 `nv`。维持**通过**（服 🟡=0，stub 🟡=0）。

---

### B4 存档、CDK、排行、全局态

重点：§1.6 结算、DataStore 只走 SystemDataStore / SystemSave、Init 内无版本迁移。

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | SystemDataStore | `src/ServerStorage/ServerSideCode/System/SystemDataStore/` | 通过 | 2026-08-28 |
| [x] | SystemCode | `src/ServerStorage/ServerSideCode/System/SystemCode.luau` | 通过（复审） | 2026-09-01 |
| [x] | SystemCDK | `src/ServerStorage/ServerSideCode/System/SystemCDK.luau` | 通过（复审） | 2026-08-31 |
| [x] | SystemRank | `src/ServerStorage/ServerSideCode/System/SystemRank/` | 通过（复审） | 2026-09-01 |
| [x] | SystemRankManager | `src/ReplicatedStorage/ClientSideCode/SystemModule/SystemRankManager/` | 通过（复审） | 2026-09-01 |
| [x] | SystemGlobalData | `src/ServerStorage/ServerSideCode/System/SystemGlobalData.luau` | 通过 | 2026-08-28 |
| [x] | SystemServerBuff | `src/ServerStorage/ServerSideCode/System/SystemServerBuff.luau` | 通过 | 2026-08-28 |
| [x] | SystemSDK | `src/ServerStorage/ServerSideCode/System/SystemSDK/` | 通过（复审） | 2026-08-31 |
| [x] | SystemTeleport | `src/ServerStorage/ServerSideCode/System/SystemTeleport.luau` | 通过 | 2026-08-28 |
| [x] | SystemUpdateLog | `src/ServerStorage/ServerSideCode/System/SystemUpdateLog.luau` | 通过（复审） | 2026-09-01 |

`SystemSave` / `SystemBag` / `SystemShop` 等已审，见 §4。

B4 无 §1.7 红牌。**B4 P0 复审（2026-08-28）：** 文件头/表名/Utils 缓存/`DataCache.Init` 已闭合。当时 SystemRankManager 改为有条件通过（黄项＞3）。当时仍不通过：SystemSDK（`isContinue` / `GivePublicPro` / `PrintProperties` 仍无块注释）。SystemRank / SystemUpdateLog 维持有条件通过。**SystemCode 2026-09-01：** CDK 接线后补 `CodeErr`、RegisterNetWork 幂等；改为通过。**SystemUpdateLog 2026-09-01：** Author 对齐、去掉 `IsServer`、Remote 幂等+限频、`ChangePlrData` 返回值与 `UpdateLogErr`；改为通过。**SystemRank 复审（2026-09-01）：** `@return` 语义、文件头、时长榜读失败不写、魔法数常量化。改为**通过**（🟡=0）。随审 SystemRank-Type。**SystemRankManager 复审（2026-09-01）：** 去掉客户端写 Top3；刷新改 Heartbeat；Init 收到 RankManager；可见文案 SetRawText。改为**通过**（🟡=0）。

**CDK 迁层（2026-08-31，§1.8 有条件通过后开工）：** SystemCDK 已迁 `ServerSideCode/System`，池走 `SystemDataStore.STORE.CDK`（CAS）；`SystemCode.RedeemCode` 接线 type 3 → `SystemBag.AddItems`，入包失败回滚。ClientSideCode 实现已删。

**SystemCDK 复审（2026-08-31）：** P0 / P1 / P2 均闭合。维持**通过**（🟡=0，🟢=0）。生成格式与默认数量已抽常量。

**SystemSDK 复审（2026-08-31）：** 私有三函数块注释已补，上次 🔴 闭合。无 §1.6 / §1.7 / `IsServer` 混写。改为**有条件通过**（🟡>3：公共 API 缺块注释、废弃注释代码、`Init` 语义占用、`spawn`、魔法数字等）。`ThinkingAnalyticsAPI` 按第三方不计入本表。B4 本批无残留不通过。

**SystemSDK P1/P2（2026-08-31）：** 公共 API 块注释；`Init` 改 `SetUserProfile`；`flush` 改 `Flush`（UpdateManager 已对齐）；删废弃空壳与 120s 空注册；私有 `_` 前缀；`task.spawn`；`Utils.RunService`；魔法数抽常量；`GetProductInfo` pcall；收据校验；商品列表同步拉取去竞态；分区注释。待复审改结论。

**SystemSDK P1/P2 复审（2026-08-31）：** 上次黄项均闭合。无 §1.6 / §1.7 / `IsServer`。改为**通过**（🟡=0，🟢=3）。`ThinkingAnalyticsAPI` 仍按第三方不计入。

---

### B5 ToolBasic 基础设施

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | GameConfig | `src/ReplicatedFirst/AllSideCode/ToolBasic/GameConfig.luau` | 通过（复审） | 2026-08-31 |
| [x] | ConfigInstance | `src/ReplicatedFirst/AllSideCode/ToolBasic/ConfigInstance.luau` | 通过 | 2026-08-28 |
| [x] | ConfigPrepare | `src/ReplicatedFirst/AllSideCode/ToolBasic/ConfigPrepare.luau` | 通过 | 2026-08-28 |
| [x] | Copy | `src/ReplicatedFirst/AllSideCode/ToolBasic/Copy.luau` | 通过 | 2026-08-28 |
| [x] | Log | `src/ReplicatedFirst/AllSideCode/ToolBasic/Log.luau` | 通过（复审） | 2026-08-31 |
| [x] | InsMgr | `src/ReplicatedFirst/AllSideCode/ToolBasic/InsMgr.luau` | 通过 | 2026-08-28 |
| [x] | AnchorRelative | `src/ReplicatedFirst/AllSideCode/ToolBasic/AnchorRelative.luau` | 通过 | 2026-08-28 |
| [x] | ChannelResultUtil | `src/ReplicatedFirst/AllSideCode/ToolBasic/ChannelResultUtil.luau` | 通过 | 2026-08-28 |
| [x] | KeyframeEval | `src/ReplicatedFirst/AllSideCode/ToolBasic/KeyframeEval.luau` | 通过 | 2026-08-28 |
| [x] | LightingTweenUtil | `src/ReplicatedFirst/AllSideCode/ToolBasic/LightingTweenUtil.luau` | 通过 | 2026-08-28 |
| [x] | NetChannel | `src/ReplicatedFirst/AllSideCode/ToolBasic/NetChannel.luau` | 通过 | 2026-08-28 |
| [x] | NetChannelMap | `src/ReplicatedFirst/AllSideCode/ToolBasic/NetChannelMap.luau` | 通过 | 2026-08-28 |
| [x] | NetMsg | `src/ReplicatedFirst/AllSideCode/ToolBasic/NetMsg.luau` | 通过（复审） | 2026-08-31 |
| [x] | NetPipeConfig | `src/ReplicatedFirst/AllSideCode/ToolBasic/NetPipeConfig.luau` | 通过 | 2026-08-28 |
| [x] | RankConfig | `src/ReplicatedFirst/AllSideCode/ToolBasic/RankConfig.luau` | 通过（复审） | 2026-09-01 |

已审 ToolBasic：AssetPaths、AssetRegistry、HumanModule、MathMgr、ObjectPoolUtil、ResRestore、TimeTransfer、TranslationHelper、ViewportFrameModule、VisibleMgr。

B5 无 §1.6/§1.7。ConfigInstance 为打表产物（禁止手改）。**B5 黄项复审（2026-08-31）：** Log 表名 / Utils.RunService / `@param` 已闭合，改为通过。GameConfig / NetMsg 维持通过。Webhook 与中英键未改。

**AssetPaths 复审（2026-08-31）：** 注释 P0 / Utils 顶部缓存 / 去掉 `IsServer` / freeze 已闭合。改为**通过**（随审 `AssetPaths-Type`）。

**ObjectPoolUtil 复审（2026-08-31）：** Author 对齐林奥宇；Utils 顶部缓存；借还守卫 / 重复归还幂等 / Clone pcall；`CLEAR_IDLE_TIME` 导出。改为**通过**（随审 `ObjectPoolUtil-Type`）。物理路径为 ToolSystem（有池索引 + Init），名单原归入 ToolBasic。

**ResRestore 复审（2026-08-31）：** Author / Utils / `ClearAssertSound` 写口 / 动画重试销毁序已闭合。改为**通过**（随审 `ResRestore-Type`）。

**HumanModule 复审（2026-08-31）：** Author 对齐林奥宇；玩家 WalkSpeed 拒绝直写（CMS 唯一写口）；去掉不存在的 `EnumMgr.EnemyProperty`/`HeroProperty`。改为**通过**（随审 `HumanModule-Type`）。

**ViewportFrameModule 复审（2026-08-31）：** Author / Created 占位已补；公共参数去 `_` 前缀；类型档更名为 `ViewportFrameModule-Type`；destroy 幂等与遗留阴影清理；魔法数抽常量。改为**通过**（随审类型档）。

**MathMgr 复审（2026-08-31）：** Author 对齐林奥宇；补 getNumStr0；公共 API 统一 camelCase；抽取 `_formatFixed`；单位表 freeze。改为**通过**（随审 `MathMgr-Type`）。

**VisibleMgr 复审（2026-09-01）：** Author 对齐林奥宇；Utils.TweenService/RunService/Players/Log；SpeedDown 拒绝玩家角色；文件头使用说明；抽取 `_forEachBasePart` / `_applyFadeNode`；粒子循环防叠；随审 `VisibleMgr-Type`。改为**通过**（🟡=1：有效行仍超 500，视觉助手不宜本轮拆目录）。

**TimeTransfer 复审（2026-09-01）：** Author 对齐林奥宇；文件头补选用关系与示例；类型档补 `FormatUnixTimestampToYMD` 与五项头；抽取 `_clampSeconds` / `_splitClock` / `_splitDuration`；`FormatTimeS` 空格与注释对齐；Unix 阈值常量化。改为**通过**（随审 `TimeTransfer-Type`）。

**TranslationHelper 复审（2026-09-01）：** Author 对齐林奥宇；文件头使用说明；类型档五项头与表名；SetRawText 写 ProximityPrompt 并解绑 `代码本地化`；`warn`→`Log.warn`；gsub `%` 转义；缺键/空串守卫。改为**通过**（随审 `LanguageCfg` / `TranslationHelper-Type`）。未改 `Language.luau`（打表产物）。

---

### B6 剩余 Manager

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | NetWorkManager（服） | `src/ServerScriptService/InitGame/Manager/NetWorkManager.server.luau` | 通过（复审） | 2026-08-31 |
| [x] | NetWorkManager（客） | `src/StarterPlayer/StarterPlayerScripts/Manager/NetWorkManager.client.luau` | 通过（复审） | 2026-08-31 |
| [x] | ResMemoryManager | `src/ServerScriptService/InitGame/Manager/ResMemoryManager.server.luau` | 通过（复审） | 2026-08-31 |
| [x] | CameraManager | `src/StarterPlayer/StarterPlayerScripts/Manager/CameraManager.client.luau` | 通过 | 2026-08-28 |
| [x] | SoundManager | `src/StarterPlayer/StarterPlayerScripts/Manager/SoundManager.client.luau` | 通过（复审） | 2026-08-31 |
| [x] | TipsManager | `src/StarterPlayer/StarterPlayerScripts/Manager/TipsManager.client.luau` | 通过 | 2026-08-28 |
| [x] | TranslationManager | `src/StarterPlayer/StarterPlayerScripts/Manager/TranslationManager.client.luau` | 通过（复审） | 2026-08-31 |
| [x] | UISetManager | `src/StarterPlayer/StarterPlayerScripts/Manager/UISetManager.client.luau` | 通过 | 2026-08-28 |
| [x] | UISetMgr | `src/ReplicatedFirst/AllSideCode/ToolSystem/UISetMgr.luau` | 通过 | 2026-08-28 |
| [x] | FootstepSound | `src/ReplicatedFirst/AllSideCode/ToolSystem/FootstepSound/` | 通过（复审） | 2026-08-31 |

B6 无 §1.6/§1.7，无 DataStore。**B6 黄项复审（2026-08-31）：** FootstepSound 改为通过。SoundManager / TranslationManager / ResMemoryManager 维持通过。未下沉 Sound/Tips 业务。

---

### B7 GuiScripts / Class / 杂项

| 做完 | 模块 | 路径 | 结论 | 日期 |
|:----:|------|------|------|------|
| [x] | DailyReward | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/DailyReward.luau` | 通过（复审） | 2026-09-01 |
| [x] | DebugSystem | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/DebugSystem/` | 通过（复审） | 2026-09-01 |
| [x] | RejoinTip | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/RejoinTip.luau` | 通过（复审） | 2026-08-28 |
| [x] | RewardsClaimWithoutWord | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/RewardsClaimWithoutWord.luau` | 通过（复审） | 2026-08-28 |
| [x] | RewardWindow | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/RewardWindow.luau` | 通过（复审） | 2026-09-01 |
| [x] | Talk | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/Talk.luau` | 通过 | 2026-08-28 |
| [x] | UpdateLog | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/UpdateLog.luau` | 通过（复审） | 2026-08-28 |
| [x] | WarnWindow | `src/ReplicatedStorage/ClientSideCode/GuiScripts/ModuleScript/WarnWindow.luau` | 通过（复审） | 2026-08-28 |
| [x] | Class | `src/ReplicatedFirst/AllSideCode/Class/Class.luau` | 通过 | 2026-08-28 |
| [x] | Struct | `src/ReplicatedFirst/AllSideCode/Class/Struct.luau` | 通过 | 2026-08-28 |
| [x] | EnumMgr | `src/ReplicatedFirst/AllSideCode/ToolSystem/EnumMgr.luau` | 通过（复审） | 2026-08-28 |
| [x] | FindPlayerByID | `src/ReplicatedFirst/AllSideCode/ToolSystem/FindPlayerByID.luau` | 通过 | 2026-08-28 |

已审 UI：NewsMail_Full、Setting、GMEmail、Feedback（2026-09-01 复审通过，随审 SystemFeedback / SystemFeedback-Type）。

B7 无 §1.6/§1.7。**B7 P0 复审（2026-08-28）：** 本地化红与 EnumMgr 头/表名已闭合。改为通过：RejoinTip、RewardsClaimWithoutWord、UpdateLog、WarnWindow、EnumMgr。改为有条件通过：DailyReward、DebugSystem（黄项＞3）。RewardWindow / Talk / Class / Struct / FindPlayerByID 维持初审。

---

## 3. 不纳入本轮

| 项 | 原因 |
|----|------|
| `SmartBone/` | 第三方库，不按项目 System 规范计分 |
| `LogicalEnemySpawnTest` | 默认可跳过；若要收口测试闸再单开 |
| 方案二次审评（如 M2 D17–D19） | 开工门禁，不是本表「规范性审查」 |

随父模块已覆盖、无需再开：

| 模块 | 随谁 |
|------|------|
| ChannelPipes、InboundDispatch | NetWork |
| SystemLightingWeather、SystemLightingCustomState | SystemLighting |
| LogicalDisplaceProfile | LogicalDisplace |
| GroupSkillModule、Presentation | BaseSkill/GroupSkill 运行时 |
| GMEmail、GmEmailWhitelist | SystemEmail |
| LightingManager | SystemLighting |
| InitSystemUI | TopBarManager（2026-07-03 已删源文件，顶栏逻辑迁入） |
| ModelFind | AssetAcquire / ResourceUtil（2026-07-02 已删兼容层源文件） |

同名但路径不同、**不能**用另一份报告顶掉的，仍按上表未审处理。客户端 `SystemBuyRoblox` 已随 2026-08-31 复审与服模块一并覆盖。

---

## 4. 已审查归档（复审队列）

最近一次报告结论。需要复审时从「不通过」开始，不要插入未审 B1–B7 之前，除非用户点名。

### 不通过（优先复审）

（无）

### 有条件通过

（无）

### 通过

CharacterControl（随审 ControlInstance / CharacterControl-Type）、HatchEffect（随审 HatchEffect-Type）、AssetAcquire（随审 AssetAcquire-Type）、CfgFind（随审 CfgFind-Type）、ResourceUtil（随审 ResourceUtil-Type）、SkillPreloadUtil、SequenceManager（随审 CatalogImages / SequenceManager-Type）、VisibleMgr（随审 VisibleMgr-Type）、AssetPaths、AssetRegistry、DeviceType、FXUtil、GetData（随审 GetData-Type）、ObjectPoolUtil、UIanima（随审 UIanima-Type / Helpers / ItemFly / NumberChange）、RayCast（随审 RayCast-Type）、RequestRateLimit（随审 RequestRateLimit-Type）、UpdateModule、WorldUtil、ObjectPoolManager、ResPreloadManager、SmartBoneManager、SystemMoonAni、SystemInput、SystemInputBind、PlayerInputClientManager、ActionArbiter、LocoStyle（随审 StyleInstance / LocoStyle-Type）、LocoStyleSets、PlayerMountClientManager、PlayerMountManager、MountNet、CameraImpulse、CameraModule、LogicalDisplace（随审 LogicalDisplaceProfile）、LogicEntityMotion、LogicalPathFollow、LogicalMoveCommand、PathFindUtil、EnemySim、RangerAi、SystemEnemy（服）、SystemEnemy（AllSide stub）、EnemyLogicalSync、EnemyLogicalTypes、LogicalAirborne、LogicalAirborneProfile、SystemLogicalEnemy、EnemyLogicalStore、EnemyManager、LogicalHitValidate、SkillDemoCast、SpeedLineFX、CombatWorld、SystemCharacterMotion、SystemPlrAttr（服）、SystemPlrAttr（AllSide）、SkillNet、SystemSummon、SkillDemoProjectile、SpaceSkill1、SkillCommon/Projectile 模板、AnimationLayer、AnimationModule、SystemDataStore、SystemCode、SystemRank（随审 SystemRank-Type）、SystemUpdateLog、SystemGlobalData、SystemServerBuff、SystemTeleport、ConfigInstance、ConfigPrepare、Copy、InsMgr、AnchorRelative、ChannelResultUtil、KeyframeEval、LightingTweenUtil、NetChannel、NetChannelMap、NetPipeConfig、RankConfig、CameraManager、TipsManager、TipsModule（随审 TipsConfig / TipsRenderer / TipsTemplateBuilder / Tips*-Type）、UISetManager、UISetMgr、Talk、Class、Struct、FindPlayerByID、DailyReward、RejoinTip、RewardsClaimWithoutWord、UpdateLog、WarnWindow、EnumMgr、GameConfig、NetMsg、NetWork、NetWorkManager（服/客）、ResMemoryManager、SoundManager、BgmManager、HeadTrack、TranslationManager、Log、FootstepSound、SystemCDK、SkillBuffUtil（服/stub）、SystemSave、SystemBag、NPCCosmeticHit（服/客）、SkillHitPresentation（服/客）、PlayerSkillManager、PlayerSkillClientManager、SystemPlayerSkill（服/客）、SystemSDK、SystemBUFF、SystemEmail、SystemShop、SystemLeaderstats、SystemDailyAward、SystemGameConfig、SystemRedPoint、SystemRecord、SystemSet、DataCache、SystemSkill（服）、SystemLighting、LightingManager、ClockManager（服/客）、AddListen、PlayerData、CharacterMotion、ResRestore、HumanModule、ViewportFrameModule、MathMgr、PlayerManager、SettingManager、TopBarManager、CharacterScripts、Animate、NewsMail_Full、ClientScript、Setting、DebugManager、DebugSystem（随审 DebugPanel / DebugI18n）、PurchaseManager、SystemBuyRoblox、ShowDetail（随审 ShowDetail-Type）、SoundModule（随审 SoundInstance / SoundModule-Type / SoundInstance-Type）、UIMgr（随审 UIMgr-Type / LengthAdjustment）、BaseSkill / GroupSkill 运行时、Meteor1、NewRoll、UtilsSystem（随审 ModuleRegistry）、PartIcles（随审 TypeRegistry / FXUtil.PartIcles 门面）、SystemMount（客）、SystemMount（服）、RewardWindow、DailyReward、SystemRankManager（随审 RankManager / SystemRankManager-Type）、UpdateManager、TimeTransfer（随审 TimeTransfer-Type）、TranslationHelper（随审 LanguageCfg / TranslationHelper-Type）、AFKManager（客）、FootStepManager（随审 FootstepSoundModule）、UIManager、Feedback（随审 SystemFeedback / SystemFeedback-Type）。

（个别模块多次复审后结论可能已变；以该次对话报告为准，本表只作索引。）

---

## 5. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-28 | 首版：从未审清单拆 B1–B7；已审归档入 §4 |
| 2026-08-28 | B1-1 规范性审查：Motion 有条件通过、Command 通过、PathFollow 有条件通过、PathFindUtil 通过、EnemySim 不通过（Init 互调 + `@return` 缺语义） |
| 2026-08-28 | EnemySim P0/P1 已修：Init 只 register；Sync 改由 UpdateManager 先 Init；`has`/`getCount` 补 `@return` 语义；`destroy` + Utils.RunService。待 B1-1 复审改结论。 |
| 2026-08-28 | B1-2 审查完成；EnemySim 复审改为通过。下一批 B2。 |
| 2026-08-28 | B1-2 P0 已修：Ranger 禁止直写 Store（`_desiredLook` → `integrate`）；SystemEnemy.Init 只置 `_inited`，EnemySim.Init 收口 UpdateManager；AllSide stub 公共注释；Types/Sync/Airborne/Profile `@param`/`@return` 语义。待复审。 |
| 2026-08-28 | B1-2 复审：架构 P0 已闭合。Types / Sync / Airborne / Profile / AllSide stub 改为通过；RangerAi、SystemEnemy（服）仍不通过（注释残留）；Store / SystemLogicalEnemy 有条件通过；EnemyManager 通过。下一批 B2。 |
| 2026-08-28 | B1-2 残留 P0 已修（注释）：RangerAi `_canInteract` `@return` 语义；服 SystemEnemy `Unregister` / `_findTargetByName` / `_buildAllowed` / `CreateLogicalEnemy opts`。待再复审改结论。 |
| 2026-08-28 | RangerAi / SystemEnemy（服）再复审改为通过；AllSide stub 维持通过。B1-2 架构 P0 与注释 P0 均闭合。下一批 B2。 |
| 2026-08-28 | B2 审查完成。通过：SystemInputBind、PlayerInputClientManager、LocoStyleSets、PlayerMountClientManager、PlayerMountManager。有条件通过：SystemMount（客/服）。不通过：SystemInput、ActionArbiter、CameraImpulse、MountNet、LocoStyle（均为注释 P0；无 §1.6/§1.7 红牌）。下一批 B3。 |
| 2026-08-28 | B2 P0 已修（注释）：SystemInput 四私有块注释 + `@param`/`@return` 语义；ActionArbiter 私有块注释 + ExpandBlendPolicy/`_resolveClaimFields` 语义；CameraImpulse `hasBind`；MountNet `_proxy`；LocoStyle `@param`/`@return` 语义。文件头 Author 对齐林奥宇。待复审改结论。 |
| 2026-08-28 | B2 P0 复审：注释红项闭合。MountNet / CameraImpulse 改为通过；SystemInput / ActionArbiter / LocoStyle 改为有条件通过（黄项＞3）。下一批 B3。 |
| 2026-08-28 | B2 P1 已修：SystemInput 公共注释/destroy/freeze/Utils/Log；SystemInputBind.Init + UpdateManager 接线 + SET 限频；ActionArbiter 查询注释/GetSnapshot 拷贝/Log；LocoStyle 控制器注释/Priority freeze；LocoStyleSets/MountAnimSets SLOT_ORDER freeze；MountNet `@param`；CameraImpulse KIND 常量；服 SystemMount Died 连接替换；客 RequestEnter 缺省走装备；Manager 失败 Log。未做：三份大文件拆分、Create 拆分、Jump→SystemInput、拥有接入 SystemSave。 |
| 2026-08-28 | B3 审查完成。通过：LogicalHitValidate、SkillDemoCast、SpeedLineFX。不通过（15）：CombatWorld、SystemCharacterMotion、SystemPlrAttr（服/AllSide）、SkillNet、SkillBuffUtil、SkillHitPresentation、NPCCosmeticHit、SystemSummon、PlayerSkillClientManager、PlayerSkillManager、SkillDemoProjectile、SpaceSkill1、SkillCommon/Projectile 模板、AnimationLayer。无 §1.6/§1.7 红牌。下一批 B4。 |
| 2026-08-28 | B3 P0 机械项已修（注释 + Manager/Utils）：`@param`/`@return` 语义、私有/公共块注释、SkillSyncLog 走 Utils、`DASH_SLOT_INDEX`、SystemMount 顶部缓存、NPCCosmeticHitPlay 顶部缓存 SystemLogicalEnemy。**未做** SkillBuffUtil / NPCCosmeticHit 拆 `IsServer`（须 §1.8 二次审评）；未下沉 Manager 跨槽/旁观业务。待复审改结论。 |
| 2026-08-28 | B3 P0 复审：注释红闭合。改为通过：CombatWorld、SystemPlrAttr（服/AllSide）、SkillNet、SystemSummon、SkillDemoProjectile、SpaceSkill1、SkillCommon/Projectile 模板、AnimationLayer。改为有条件通过：SystemCharacterMotion、SkillHitPresentation（黄项＞3）。仍不通过：SkillBuffUtil、NPCCosmeticHit、PlayerSkillClientManager、PlayerSkillManager。下一批 B4。 |
| 2026-08-28 | B4 审查完成。通过：SystemDataStore、SystemGlobalData、SystemServerBuff、SystemTeleport。有条件通过：SystemCode、SystemRank、SystemUpdateLog。不通过：SystemCDK（ClientSideCode + 直连 DataStore / §1.6 兑换写口）、SystemRankManager（Init 互调 DataCache）、SystemSDK（无文件头 / 表名 module / Utils 链 / 私有无注释）。无 §1.7 红牌。无 Init 内版本迁移。下一批 B5。 |
| 2026-08-28 | B4 P0 机械项已修：SystemSDK 文件头（Author 林奥宇）+ 表名 SystemSDK + 顶部缓存 WorldUtil/UpdateModule；SystemCDK 文件头 + 表名 SystemCDK；SystemRankManager 去掉 DataCache.Init（走 DataCache 惰性 _ensureInit）。**未做** CDK 迁层/发奖接线（须 §1.8）；未补 SystemSDK 私有块注释。待复审改结论。 |
| 2026-08-28 | B5 审查完成。通过：ConfigInstance（打表产物）、ConfigPrepare、Copy、InsMgr、AnchorRelative、ChannelResultUtil、KeyframeEval、LightingTweenUtil、NetChannel、NetChannelMap、NetPipeConfig、RankConfig。有条件通过：Log。不通过：GameConfig（无标准头 / 表名 module / 未 freeze）、NetMsg（文件头缺 Author/Created/Last Modified）。无 §1.6/§1.7。下一批 B6。 |
| 2026-08-28 | B4 P0 复审：头/表名/Utils/`DataCache.Init` 闭合。SystemRankManager 改为有条件通过。仍不通过：SystemCDK（架构）、SystemSDK（私有无注释）。CDK 迁层未做。下一批仍为 B6。 |
| 2026-08-28 | B6 审查完成。通过：CameraManager、TipsManager、UISetManager、UISetMgr。不通过：NetWorkManager 服/客（文件头缺项 + `Utils.X` 链）、ResMemoryManager（缺 `Module:`）、SoundManager（`@param` 缺 `-` 语义）、TranslationManager（私有无块注释）、FootstepSound（init 文件头）。无 §1.6/§1.7。下一批 B7。 |
| 2026-08-28 | B7 审查完成。通过：Talk、Class、Struct、FindPlayerByID。有条件通过：RewardWindow。不通过：DailyReward / DebugSystem / RejoinTip / RewardsClaimWithoutWord / UpdateLog / WarnWindow（本地化硬编码或 Key 非字面量非 Zh*；UpdateLog 另有 `Utils.CfgFind` 链）、EnumMgr（无标准头 / 表名 `module`）。无 §1.6/§1.7。顶层未审清零。 |
| 2026-08-28 | B7 P0 已修：DailyReward `SetText(ZhName, awardCfg.ZhName)` + `签到天数`；DebugSystem 静态文案 SetText/TranslateByKey；RejoinTip `正在等待更新`；RewardsClaimWithoutWord `当前等级`；UpdateLog 顶部缓存 CfgFind + `cfg.ZhUpdateText`；WarnWindow 动态文案改 SetRawText（调用方须先 TranslateByKey）；EnumMgr 五项头（Author 林奥宇）+ 表名 EnumMgr。Key 已入 LocalizationTable.xlsx 并打表。待复审改结论。 |
| 2026-08-28 | B7 P0 复审：本地化红与 EnumMgr 头/表名闭合。改为通过：RejoinTip、RewardsClaimWithoutWord、UpdateLog、WarnWindow、EnumMgr。改为有条件通过：DailyReward、DebugSystem（黄项＞3）。 |
| 2026-08-31 | B5/B6 P0 机械项已修：GameConfig 五项头 + 表名 + deep freeze；NetMsg 补头字段；NetWorkManager 服/客补头并顶部缓存 Utils；ResMemoryManager 补 `Module:`（Author 林奥宇）；SoundManager `@param`/`@return` 补 `-` 语义；TranslationManager 三处私有块注释；FootstepSound init 五项头。未改结论，待复审。 |
| 2026-08-31 | B5/B6 P0 复审：机械红闭合。改为通过：GameConfig、NetMsg、NetWorkManager 服/客、ResMemoryManager、SoundManager、TranslationManager。改为有条件通过：FootstepSound（黄＞3）。Log 维持有条件通过。 |
| 2026-08-31 | B5/B6 黄项已修：Log 表名 + Utils.RunService + `@param`；FootstepSound 表名/destroy/注释/去掉未用服务；TranslationManager 未用缓存、`_` 前缀、CODE_TAG；SoundManager Log.warn + `_onStopSound`；ResMemoryManager Utils.SoundService。未改 Webhook/中英键，未下沉 Sound/Tips。待复审改结论。 |
| 2026-08-31 | B5/B6 黄项复审：机械黄闭合。改为通过：Log、FootstepSound。维持通过：GameConfig、NetMsg、NetWorkManager 服/客、ResMemoryManager、SoundManager、TranslationManager。B5/B6 本批无残留不通过。 |
| 2026-08-31 | CDK §1.8 有条件通过后开工：SystemCDK 迁 ServerSideCode；池走 SystemDataStore.Update CAS；SystemCode 接线 type 3 发奖，入包失败回滚；删 ClientSideCode 实现；§5.2 取消 CDK 直连 DS 排除。待复审。 |
| 2026-08-31 | SystemCDK 复审：不通过。架构/§1.6/DS 网关闭合。P0：Redeem 返回配表奖励数组活引用。黄：表定义早于 require、poolConfig 注释缺字段、GenerateAllPoolsCDK 全失败仍 true。 |
| 2026-08-31 | SystemCDK P0 已修并复审：建池 + Redeem 深拷贝奖励列表。改为通过（🟡=3）。 |
| 2026-08-31 | SystemCDK P1 已修并复审：表顺序、GenerateAllPoolsCDK 零成功 false、CdkPoolConfig 字段形。维持通过（🟡=0）。 |
| 2026-08-31 | SystemCDK P2 已修并复审：段数/每段字符/去重倍数/默认每池数量抽常量。维持通过（无残留黄绿项）。 |
| 2026-08-31 | SkillBuffUtil §1.8 有条件通过后开工：写口迁 ServerSideCode；stub 只读窗；Utils 分流；Init 不互调 CombatWorld。待复审。 |
| 2026-08-31 | SkillBuffUtil 复审：架构/`IsServer`/§1.6/§1.7 闭合。改为通过（服 🟡=2：Shutdown 未用 destroy、Tick FindFirstChild）。 |
| 2026-08-31 | SkillBuffUtil P1 已修：destroy + Tick 缓存 nv。待复审改黄项。 |
| 2026-08-31 | SkillBuffUtil P1 复审：黄项闭合。维持通过（🟡=0）。 |
| 2026-08-31 | SystemSave 复审：仍不通过。初审注释/头/Utils/§1.6/§1.7 已闭合。P0：`LAST_VERSION` 只读不写，默认 `""` 使 `_tryAcceptSessionData` 在 `IsInServer==1` 时几乎总是接受，双会话等待与过期阈值失效。 |
| 2026-08-31 | SystemSave P0 已修：去掉版本号放行；互斥只看 IsInServer + LastSaveTime；接档/保存回写 LAST_VERSION。 |
| 2026-08-31 | SystemSave 再复审：P0 闭合。改为有条件通过（🟡>3：接档未立刻 Set、ChangeStat 共享引用/未校验、模板未 freeze、AutoSave 解析键、Kick 硬编码等）。 |
| 2026-08-31 | SystemSave P1 已修：接档后立即 Set；ChangeStat 深拷贝+顶层键白名单+可序列化校验；模板 freeze；ParsePlayerKey；`_sessionData`/`_versionID`；Kick 常量；注释字段形。 |
| 2026-08-31 | SystemSave P1 复审：P1 闭合。改为通过（🟡=2：FixData 未按版本门控；接档 Set 失败不阻断进服）。 |
| 2026-08-31 | SystemBag 复审：仍不通过。初审 P0（函数内 require、Init 越界）已闭合：拆目录模块、UseScript 预加载、`SetupPlayerBag`。新 P0：五项头缺 Author；`Utils.NetWork`/`Utils.ServerStorage` 未顶部缓存。 |
| 2026-08-31 | SystemBag P0 已修：六份文件头补 Author 林奥宇（子模块补 Created/Last Modified）；顶部缓存 NetWork / ServerStorage。待复审改结论。 |
| 2026-08-31 | SystemBag P1 已修：去掉进服 init 抬堆叠与背包容量启发式；修改类 true/false+errorCode；UseItem 失败回滚；AddData 同步 Value；记录事件移到写入后；去掉 IsServer；RemoveTradeItem 查锁与数量。调用方 SystemCode / SystemEmail / SystemBuyRoblox 已对齐 AddItems。待复审。 |
| 2026-08-31 | SystemBag 复审：P0/P1 闭合。改为通过（🟡=1：RemoveTradeItem 仍返回条目拷贝/nil，未走 errorCode）。 |
| 2026-08-31 | SystemBag P1/P2 已修：RemoveTradeItem 改为 true/copy + false/errorCode；展示叠加锁定交易常量进 EnumMgr；删 EVENT_RECORD_*；update/createValueInstances 按名缓存 Value。GRANT_AWARD 仍作入包 Bindable 入口保留。待复审。 |
| 2026-08-31 | SystemBag P1/P2 复审：黄绿项闭合。维持通过（🟡=0；🟢=1：ItemScript 加载期 require(child)）。 |
| 2026-08-31 | B3 不通过三项复审：NPCCosmeticHit / PlayerSkillClientManager / PlayerSkillManager 维持不通过。无 §1.6/§1.7 新红。拆分须 §1.8。 |
| 2026-08-31 | B3 三项拆层方案：NPCCosmeticHit 按 SkillBuffUtil 分流；技能运行时新建 SystemPlayerSkill（不并 SystemSkill）。§1.8 有条件通过。未开工。 |
| 2026-08-31 | 按方案开工：NPCCosmeticHit 服模块 + Utils 分流；SystemPlayerSkill 服/客；Manager 只转发；飞行拒释补 FireClient；EnumMgr.SkillSlot。待复审。 |
| 2026-08-31 | B3 拆层复审：NPCCosmeticHit 服/客、PlayerSkillManager、PlayerSkillClientManager 改为通过。SystemPlayerSkill 服/客有条件通过。B3 无残留不通过。 |
| 2026-08-31 | SystemPlayerSkill P1 已修：离场 destroy 槽、抽出 `_onUnbind`/`_watchSlot`/`_findFromRuntime`、Init 挪到 return 前、OnRelease 字段形、旁观 destroy Log.warn、`EnumMgr.SkillFolder`。长路径 require 按方案保留。改为通过。 |
| 2026-08-31 | SystemSDK 复审：私有三函数块注释已补。改为有条件通过（🟡>3）。B4 无残留不通过。 |
| 2026-08-31 | SystemSDK P1/P2 已修：公共注释、SetUserProfile/Flush、删废弃空壳、私有前缀、task.spawn、常量、pcall、收据校验、商品列表去竞态。UpdateManager 改调 Flush。待复审。 |
| 2026-08-31 | SystemSDK P1/P2 复审：黄项闭合。改为通过（🟡=0）。 |
| 2026-08-31 | SystemBUFF 复审：初审文件头/表名 P0 已闭合。改为有条件通过（🟡=4：Utils.Players、魔法名、每秒全量拷贝、ChangePlrData 未检查）。 |
| 2026-08-31 | SystemBUFF P1/P2 已修：Utils.Players、GetPlayers、ChangePlrData 先写后镜像、EnumMgr.PlayerInst、isOver 缺省 1、缓存 Folder/房间号、GetPlrDataByKey、BuffMap 类型。待复审。 |
| 2026-08-31 | SystemBUFF P1/P2 复审：黄项闭合。改为通过（🟡=0）。 |
| 2026-08-31 | SystemEmail 复审：名单「不通过」与「有条件通过（第三次）」双挂已收口，只留本结论。相对第三次：`AddItems` 已按 true/false；无私自 Remote / 直连 DS / Init 内迁移 / §1.6 / §1.7。改为**通过**（🟡=3：标记后离线不回滚、礼品路径不检查返回、老存档 ChangePlrData 未检查）。`GMEmail` / `GmEmailWhitelist` 维持随审；`NewsMail_Full` 仍不通过、本刀未审。 |
| 2026-08-31 | SystemEmail P1 已修：标记后离线/读 DS 失败回滚 `IsSynced`；回滚失败用 `GmEmailErr.SyncRevertFailed`（面板不再说重进到账）；礼品路径认 `BuyItemEnd` / GamePass 存档；老存档 `ChangePlrData` 检查返回值。DebugManager / GMEmail 已对齐错误码。待复审改黄项。 |
| 2026-08-31 | SystemEmail P1 复审：上次三条黄项均闭合。维持**通过**（🟡=0）。 |
| 2026-08-31 | SystemEmail P2 已修：老存档先写掉待发再入包；`Utils.Struct`；UTF-8 码点边界截断。待复审。 |
| 2026-08-31 | SystemEmail P2 复审：三条绿项闭合。维持**通过**（🟡=0，🟢=0）。 |
| 2026-08-31 | SystemShop 复审：初审注释/表名/魔法数/Log P0 已闭合。无私自 Remote / 直连 DS / Init 内迁移 / §1.6 / §1.7。改为**通过**（🟡=3：先写拥有再入包且不检查 AddItems；ChangePlrData 未检查；修改类 API 返回值不一致）。 |
| 2026-08-31 | SystemShop P1 已修：先入包再写拥有；认 AddItems / ChangePlrData；修改类 `true`/`false`+`EnumMgr.ShopErr`。SystemBuyRoblox / SystemEmail 已对齐返回值。待复审改黄项。 |
| 2026-08-31 | SystemShop P2 已修：`EnumMgr.PlayerInst.GamePassFolder`；InitValue 只同步镜像；Reset 标为仓内无调用的运营写口。BagValueSync / PlayerMirror 读侧已对齐 Folder 名。 |
| 2026-08-31 | SystemShop P2 复审：上次三条黄项与五条绿项均闭合。维持**通过**（🟡=0，🟢=0）。 |
| 2026-08-31 | SystemLeaderstats 复审：初审文件头已有。P0：`EnumMgr.ItemID.Level` 不存在，展示恒为 0。已修：数据源改 `ItemID.Coin`、列名 Coin、Folder、Utils.Players、缓存+离场清理、值未变不写。改为**通过**（🟡=0）。§1.8 残留：列名/数据源从虚构 Level 改为 Coin，无存档/Remote/§1.6/§1.7 变更。 |
| 2026-08-31 | SystemDailyAward 复审并修 P0/P1/P2：Author 对齐林奥宇；`LastDailyTime=0` 不再当空；缺配置不结算；先写标志再入包、失败回滚；下一关缺配置不回退当天；`领取成功` 入表；去 IsServer；限频；`DailyAwardErr`。改为**通过**（🟡=0）。§1.8 残留：结算顺序与回滚，无新 Remote/存档字段、无 §1.6/§1.7。 |
| 2026-08-31 | SystemRecord 复审并修 P0/P1/P2：Author 对齐林奥宇；写口只经 ChangePlrData 且检查返回；Utils.Players/Log；镜像缓存+离场清理；RegisterNetWork 幂等；Bindable 失败打日志；`EnumMgr.PlayerInst.RecordFolder`。改为**通过**（🟡=0）。§1.8 残留：无新 Remote/存档字段、无 §1.6/§1.7；写口仍走 PlayerData.ChangePlrData。 |
| 2026-08-31 | SystemGameConfig 复审并修 P0/P1/P2：顶部缓存 GameConfig/Copy；GetValue 对 table 深拷贝；文件头使用说明；`@param` 格式；ClearCache 标明不能热替换源文件；非法嵌套路径静默 nil。改为**通过**（🟡=0）。§1.8 残留：查询面拷贝，无 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | SystemRedPoint 复审并修 P0/P1/P2：Author 对齐林奥宇；`@param`/`@return` 补语义与表字段形；未知红点名拒绝写入；先 ChangePlrData 再镜像；计数钳制 ≥0；Init 只刷镜像；Folder 名进 `EnumMgr.PlayerInst.RedPointFolder`。改为**通过**（🟡=0）。§1.8 残留：白名单与写序，无新 Remote/存档字段、无 §1.6/§1.7。 |
| 2026-08-31 | SystemSet 复审并修 P0/P1/P2：Author 对齐林奥宇；去掉 IsServer；按 settingConf.range 钳制；认 ChangePlrData；`SetErr`；限频；去掉不存在的 SystemArmor/SystemHumanRace 视觉调用。改为**通过**（🟡=0）。§1.8 残留：钳制与错误码，无新 Remote/存档字段、无 §1.6/§1.7。 |
| 2026-08-31 | DataCache 复审并修 P0/P1/P2：Author 对齐林奥宇；去掉 `IsServer`，Init 由 UpdateManager 注入 cacheRoot/sweep；Utils 顶部缓存服务；属性名/缩略图枚举抽常量；抽出 `_findCacheEntry`/`_writeCacheEntry`。改为**通过**（🟡=0）。§1.8 残留：未拆成服/客两份模块（双端 API 相同）；无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | SystemSkill 复审并修 P0/P1/P2（仅服装备权威）：`@return` 补 `-` 语义；`GetEquippedSkills` 深拷贝（初审已有）；LEGACY 迁移已在 FixData；`SkillFolder`/`SkillErr`；认 ChangePlrData；Init 拆私有函数；专用槽查找表。改为**通过**（🟡=0）。客运行时 `BaseSkill`/`GroupSkill` 从随审拆出，维持不通过（ClientSideCode 服权威 / `IsServer` / `PlayerAimSync` 私连 Remote，须 §1.8）。§1.8 残留：Folder 名与错误码收口，无新 Remote/存档字段、无 §1.6/§1.7。 |
| 2026-08-31 | SystemBuyRoblox 复审：初审分层/函数内 require/长路径/文件头/`@param` P0 已闭合（ToolSystem 已拆服 System + 客 SystemModule）。无私自 Connect Remote / 直连 DS / Init 内迁移 / §1.6 / §1.7。维持**不通过**。P0：购买/赠送 Key 未入 LocalizationTable；客「赠送确认」未 TranslateByKey 即交 WarnWindow SetRawText。 |
| 2026-08-31 | SystemLighting 复审并修 P0/P1/P2（含随审 Weather / CustomState / LightingManager）：初审闪帧/`States` 暴露已闭合；补 `_isLive` 块注释；Author 对齐林奥宇；Cloud `Enabled=false` 先写目标再 hide；`_classifyApplyOutcome` 收口；分区注释；删 EffectTweenUtil / `applyEnabledFromTemplate`；DebugAttrs freeze。改为**通过**（🟡=0）。§1.8 残留：Cloud 关闭写序与 PostEffect 对齐，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | CameraModule 复审并修 P0/P1/P2：补 `@param`/`@return` 语义；抽出 CameraConfig / CameraShakeApi，init 434 / ShakeRuntime 498 有效行；Utils.Players/RunService/UserInputService/Log；CameraMode.bind；层满 `LAYER_FULL`；魔法数常量化。改为**通过**（🟡=0）。§1.8 残留：拆子模块与错误码，无新 Remote/存档/§1.6；CFrame 仍仅本模块合成写。 |
| 2026-08-31 | NetWork 审查并修 P0/P1/P2：补 `@param`/`@return` 语义与文件头示例；入站校验非空 string msgName / 管道实例匹配；限频未就绪失败关闭；FireClient 校验在线玩家；InvokeServer 门面 pcall；去掉未使用的 `NetWork.NetMsg` 再导出。拆目录模块（init 121 / ChannelPipes 162 / InboundDispatch 331 有效行）。改为**通过**（🟡=0）。§1.8 残留：拆子模块与入站收紧，无新 Remote/存档字段、无 §1.6/§1.7；`IS_SERVER` 仅管道绑定与发送门控。 |
| 2026-08-31 | AnimationModule 复审并修 P0/P1/P2：顶部缓存 ActionArbiter；PlayAnimByModel 补 actionClaim `@param`；Author 对齐林奥宇；CleanupAnimator 断 Marker/占闸连接；ResumeAll 不再 Play 未冻轨；StopAll 清冻结 Heartbeat；类型档补齐 API。改为**通过**（🟡=0）。§1.8 残留：冻结 Resume/连接清理，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | PlayerData 审查并修 P0/P1/P2（随审 `PlayerData.client`、`PlayerData-Type`）：Author 对齐林奥宇；查询/监听深拷贝不暴露缓存；增量 nil 删除路径；RegisterNetWork 幂等+限频；公共参数去 `_` 前缀；客户端只读 LocalPlayer；入服脚本改 ListenClientSync+重试。改为**通过**（🟡=0）。§1.8 残留：保留 AllSide 门面的 IS_CLIENT/IS_SERVER 分流（与 NetWork 同类），无新 Remote/存档字段、无 §1.6/§1.7。 |
| 2026-08-31 | AddListen 复审并修 P0/P1/P2：无架构红牌。补文件头使用说明（Author 林奥宇）；区块顺序；Utils.UserInputService；NumValueAdd 扩 ValueBase；AddMouseClick 正名+别名；GuiButton；guard+Log；Hover 实时 MouseEnabled 与触屏 Up 去重。改为**通过**（🟡=0）。§1.8 残留：单文件规范修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | SystemBuyRoblox 复审并修本地化 P0：Key 入 LocalizationTable 并打表；客「赠送确认」先 TranslateByKey 再交 WarnWindow；`获得GamePass` args 改 `{ { cfg.ZhName } }`。改为**有条件通过**（🟡=7：AddItems 失败仍提示成功、收据先发货后写历史、QBBuy 死 API/`ItemID.Robux`、返回值无 errorCode、GiftPlayer 无服写口、CANCEL_GIFT 无限频、Merchant 魔法串）。 |
| 2026-08-31 | SystemBuyRoblox P1 已修：AddItems 失败不提示成功；PurchaseHistory 先 Update 占位再发货、失败 Remove；删 QBBuy / Merchant / SeasonCard 死分支；`true`/`false`+`BuyRobloxErr`；`SET_GIFT` 服写 GiftPlayer（只收 userId）；SET/CANCEL 限频 3/5s。待复审改黄项。 |
| 2026-08-31 | BaseSkill/GroupSkill §1.8：拆层方案见 [`BaseSkillGroupSkill拆层方案.md`](BaseSkillGroupSkill拆层方案.md)。二次审评 **有条件通过**。未开工。运行时模块维持**不通过**（架构 P0 未落地）。 |
| 2026-08-31 | BaseSkill/GroupSkill 拆层落地：服运行时迁 `SystemSkillRuntime`；HitResolver / PlayerAimSync 按端分流；瞄准走 `PLAYER_AIM_SAMPLE`；Hitbox 去掉 `IsServer`。待规范性复审改结论。 |
| 2026-08-31 | CharacterMotion 复审并修 P0/P1/P2：补 VelocityMotor 私有/公共 `@param`/`@return`；Constants/Recipes freeze + `GetConstant`；Begin 拆收养路径；Humanoid 缓存；`Log.warn`；魔法数常量化。改为**通过**（🟡=1：VelocityMotor 有效行仍超 500，电机单一写口不宜拆）。§1.8 残留：注释/封装/常量，无新 Remote/存档/§1.6；平面速度仍只经 Compose 写 LV。 |
| 2026-08-31 | HumanModule 复审并修 P0/P1/P2：Author 对齐林奥宇；玩家 WalkSpeed 拒绝直写（CMS 唯一写口）；去掉不存在的 `EnumMgr.EnemyProperty`/`HeroProperty`；Utils.Players/Log；修改类 `true`/`false`+errorCode；类型桩补 `IsPlrCharAlive`/`GetHeldItemType`。改为**通过**（🟡=0）。§1.8 残留：NPC 遗留直写仍保留且仓内无调用；迁 CMS Modifier 须另开方案。无新 Remote/存档/§1.6。 |
| 2026-08-31 | AssetPaths 复审并修 P0/P1/P2（随审 `AssetPaths-Type`）：Author 对齐林奥宇；`_readFolderCache` 补第二 `@return`；顶部缓存 Utils.ReplicatedStorage/Log；`rawget` 取 ServerStorage（去掉 `IsServer`）；`Log.warn`；GetCatalog 入参守卫；抽出 `_getCachedAssetsRoot`；路径常量 + freeze；类型档补头与 API 注释。改为**通过**（🟡=0）。§1.8 残留：单文件规范修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | ObjectPoolUtil 复审并修 P0/P1/P2（随审 `ObjectPoolUtil-Type`）：Author 对齐林奥宇；顶部缓存 Utils.ReplicatedStorage/Log；入参守卫 + Clone pcall；重复归还幂等；`add`/`back` 改 true/false+Err；导出 CLEAR_IDLE_TIME（Manager 对齐）；空闲清理走索引 + Destroy。改为**通过**（🟡=0）。§1.8 残留：单文件规范修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | ResRestore 复审并修 P0/P1/P2（随审 `ResRestore-Type`）：Author 对齐林奥宇；服务走 Utils；`ClearAssertSound` 处理 Sound 自身+后代且防二次覆盖；动画预加载失败后等重试再销毁；去掉不存在的 `ResLoadedNumber` 计数；`PreloadAssets` 正名+别名；`Log.warn`；抽出 `_swapStringProp`。改为**通过**（🟡=0）。§1.8 残留：单文件规范与卸载写口收紧，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | ViewportFrameModule 复审并修 P0/P1/P2（随审 `ViewportFrameModule-Type`）：Author/Created 对齐林奥宇；公共参数去 `_` 前缀；类型档更名与表名；destroy 幂等；遗留阴影清理；魔法数抽常量；无效视口 `new` 返回 nil。改为**通过**（🟡=0）。§1.8 残留：单文件规范与生命周期守卫，无新 Remote/存档/§1.6/§1.7；只写离屏视口相机。 |
| 2026-08-31 | MathMgr 复审并修 P0/P1/P2（随审 `MathMgr-Type`）：Author 对齐林奥宇；补文件头使用说明与 `getNumStr0`；API 统一 camelCase；抽取 `_formatFixed` / 进位溢出；单位表 freeze；`isInRate` 守卫 max<=0。改为**通过**（🟡=0）。§1.8 残留：单文件规范与展示 API 正名，无新 Remote/存档/§1.6/§1.7；`rollPoolItemByWeight` 仍留在 ToolBasic。 |
| 2026-08-31 | SettingManager 复审并修 P0/P1/P2：去掉旧档 0~1 换算（新档 1% 不再当成满音量）；Author 对齐林奥宇；`EnumMgr.PlayerInst.SettingFolder`；WaitForChild 超时+类型校验+Log；渐变秒守卫；ChildAdded 纳入后到组。改为**通过**（🟡=0）。§1.8 残留：LocalScript 编排修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | InitSystemUI 复审：源文件 `ClientScript/InitSystemUI.client.luau` 已于 2026-07-03 删除，顶栏逻辑迁入 `TopBarManager`。无代码可修。从 §4 不通过摘除，改随审 TopBarManager。 |
| 2026-08-31 | PlayerManager 审查并修 P0/P1/P2：顶部缓存 SystemSkill/SystemPlrAttr 与 Players/CollectionService；PlayerInst/PlayerAttr 收口镜像名；WaitForChild 超时；ResetPlayer 仍清连接；拆外观/碰撞/属性 Init。改为**通过**（🟡=0）。§1.8 残留：入服 AFK 仍一次性 PivotTo（非电机）；外观 AssetId 仍本脚本常量。无新 Remote/存档/§1.6。 |
| 2026-08-31 | CharacterScripts 审查并修 P0/P1/P2：顶部缓存 SystemGameConfig；服务走 Utils；Author 对齐林奥宇；HeadLock 字段 `_` 前缀 + CameraSubject 先置位；Touch 不再读 PlayerGui；Bind `Log.warn`；连接 AncestryChanged 清理。改为**通过**（🟡=0）。§1.8 残留：规范与生命周期修复，无新 Remote/存档/§1.6；WalkSpeed 仍只经 CMS 写。 |
| 2026-08-31 | TopBarManager 审查并修 P0/P1/P2：玩家可见文案走 TranslationHelper（Key 入 LocalizationTable 并打表）；Author 对齐林奥宇；删左上角反馈死代码；金币 Changed 重绑前 Disconnect；魔法数常量化。改为**通过**（🟡=0）。§1.8 残留：单文件规范与本地化，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | NewsMail_Full 审查并修 P0/P1/P2：`updatelogConf.TagTitle` 改 `ZhTagTitle`（Excel 打表）；Mail GM 文案改 SetText_UnTrans；Struct/LocalPlayer/TweenService 走 Utils；表名与分区；open 连接 DisconnectAll；delay/滑出 Wait 加 Visible 守卫。改为**通过**（🟡=0）。§1.8 残留：配置列改名与 UI 规范修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-08-31 | DebugManager 审查并修 P0/P1/P2（服+客；随修 DebugSystem 动态按钮 SetText、EnumMgr.DebugBagOp/DebugSpecialOp/GmEmailErr）：无 §1.6/§1.7 红牌。Author 对齐林奥宇；抽出 `_guardDebugRemote`；认 AddItems/RecordCover；清档失败回滚 ResetPlayer 不踢；msgType/操作名/限频键进枚举与 NetMsg；动态按钮 Key 入 LocalizationTable 并打表。待复审改结论。§1.8 残留：清档仍走 SystemDataStore（下沉 SystemSave 须另开方案）；Kick 文案未按玩家 Locale 翻译。 |
| 2026-08-31 | ClientScript 审查并修 P0/P1/P2（InitMain / BuffUpdate / InitEvent / InitTop / GMEmail）：无 §1.6/§1.7。Author 对齐林奥宇；顶栏 `活动通知` 入表；SetRawText 数量/倒计时；Utils.Players/Log；AddMouseClick；IsNewPlayer 超时；PromptRsvp pcall；GMEmail 白名单后才加载 Utils。改为**通过**（🟡=0）。§1.8 残留：LocalScript 规范与本地化，无新 Remote/存档；服务器 BUFF 倒计时由 UniqueTimeString 英文单位改为 FormatTime。 |
| 2026-08-31 | Animate 审查并修 P0/P1/P2：顶部缓存 SystemGameConfig/LocalPlayer/TweenService/SoundService；Author 对齐林奥宇；私有/公共 `@param`/`@return` 语义；抽出 `_applyGaitVisual`；FOV 重试协程 destroy 时 cancel。改为**有条件通过**。未拆 GroundLocomotion/AnimateModule（有效行＞500，运动写口不宜本轮拆）。§1.8 残留：注释/Utils/封装，无新 Remote/存档/§1.6；ShiftLock 偏航仍只经 ActionArbiter.facingOwner 写 HRP。 |
| 2026-08-31 | Setting 审查并修 P0/P1/P2（init / AllUI / SettingRowBind / SettingDropdown / SlideBar / KeybindPanel）：玩家可见文案走 SetText/TranslateByKey（Key 入 LocalizationTable 并打表）；Author 对齐林奥宇；服务走 Utils（含 GuiService）；表名 Setting/SlideBar/AllUI；拆 `_buildSettingUI` / `_beginDragSession` / KeybindPanel chrome。改为**通过**（🟡=0）。§1.8 残留：单文件规范与本地化，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | DebugManager 复审：上次 P0/P1/P2 闭合。改为**通过**（🟡=1：Kick「重新进入」未按玩家 Locale）。清档仍走 SystemDataStore，维持 §1.8 残留。 |
| 2026-09-01 | SystemBuyRoblox 复审：上次 P1 七条黄项闭合。改为**通过**（🟡=3：`GetService("Players")`、SET/CANCEL 只回 boolean、部分入包失败不回滚）。无 §1.6/§1.7。 |
| 2026-09-01 | BaseSkill/GroupSkill 复审：架构/`IsServer`/私连 Remote/§1.6/§1.7 闭合；ClientSideCode 已无服 façade。维持**不通过**（注释 P0：`resolveInterruptionPriorityFromModule`/`HitBroken`/`_clear*` 等无私有块注释；多处 `@param` 缺 `类型 - 语义`；`getInterruptionPriority` 无公共注释）。 |
| 2026-09-01 | BaseSkill/GroupSkill 修注释 P0：补 `resolveInterruptionPriorityFromModule` / `getInterruptionPriority` / `_clear*` / `HitBroken` 块注释；`GroupSkillServer.new` 改为 `initData` 字段形；SystemSkillRuntime + 服 HitResolver 查询 API 统一 `@param 名称 类型 - 语义`。改为**有条件通过**（🟡=6：私有无 `_` 前缀、`HitResolver` 有效行＞500、`Tick`/`applyHit` 超长、`print`/`warn`、客瞄准 Heartbeat 未 Disconnect）。无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | BaseSkill/GroupSkill 修残留黄项：私有加 `_`；`print`/`warn`→`Log`；`Tick`/`applyHit` 拆段；服 `HitResolver` 拆 Query/ApplyHit/LogicalEnemy/Destructible/Knockback（写口仍只留 façade）；客瞄准 Heartbeat 离场 Disconnect。改为**通过**（🟡=0）。§1.8 残留：击退仍只经 SystemCharacterMotion；可破坏物残骸短时 BodyVelocity 不写角色。无新 Remote/存档/§1.6。 |
| 2026-09-01 | SoundModule 审查并修 P0/P1/P2（随审 SoundInstance、SoundModule-Type、SoundInstance-Type）：补 `@param`/`@return` 语义；服务走 Utils；`Log.warn`；播放/停止守卫；stopSound 忽略 Cancelled Tween；3D Part 抽取；组音量 Tween 互斥；模板预索引。改为**通过**（🟡=0）。§1.8 残留：规范与守卫，无新 Remote/存档/§1.6/§1.7；`IsClient` 仅客户端副作用守卫。 |
| 2026-09-01 | DeviceType 审查并修 P0/P1/P2（随审 `DeviceType-Type`）：无 §1.6/§1.7。Author 对齐林奥宇；文件头补端侧约束与 SystemInput 边界；IsGamePad 块注释；`--!strict` 与返回类型；类型档补五项头。改为**通过**（🟡=0）。§1.8 残留：单文件规范与注释，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | UpdateModule 审查并修 P0/P1/P2（随审 `UpdateModule-Type`）：无 §1.6/§1.7。表名改 UpdateModule；`_invokeEntryCallback` `@param` 补语义；INTERVAL freeze；Utils.RunService/Log；Register/Start 注释补字段与副作用；`--!strict`；spawn 类型守卫；pcall 抽 `_runCallback`；类型档改声明桩。改为**通过**（🟡=0）。§1.8 残留：单文件规范修复，无新 Remote/存档/§1.6/§1.7；`IsServer` 仅端侧门控。 |
| 2026-09-01 | GetData 审查并修 P0/P1/P2（随审 `GetData-Type`）：无 §1.6/§1.7 红牌。Combat 补齐私有/公共块注释与 `@param`/`@return` 语义；Author 对齐林奥宇；默认属性查找表；纯度公式收口到 Combat；Utils.RunService/Players/Log；Setting/GamePass 走 PlayerInst；NumberValue `IsA`；类型档补 Combat 与门面占位 API。改为**通过**（🟡=0）。§1.8 残留：目录模块规范修复；`IsClient` 仅 Instance/PlayerData 回退；SetPlrAttr 仍为会话 Instance 写助手段（权威承伤在服 SystemPlrAttr）；GetDmg.finalDamage 仍由调用方保证来自服预结算。无新 Remote/存档字段。 |
| 2026-09-01 | WorldUtil 审查并修 P0/P1/P2（随审 `WorldUtil-Type`）：§1.6 客上报 owner 改为 VIP 用 `PrivateServerOwnerId`、预留服仅自称且只写一次；Official 同 PlaceId 优先；`RegisterNetWork` 内 `IS_SERVER` 门控；`NetMsg` 常量；抽出私有 handler 与完整注释。改为**通过**（🟡=0）。§1.8 残留：预留服完整归属须 TeleportData，另开方案；`PLACE_ID_INFO` 仍需配置独立 Dev PlaceId。 |
| 2026-09-01 | TipsModule 审查并修 P0/P1/P2（随审 TipsConfig / TipsRenderer / TipsTemplateBuilder / Tips*-Type）：载具合成改 `CfgFind`+`itemCfg.ZhName`；服务走 Utils；`destroy` 归还对象池；描边厚度常量化；满条回收改维护表；抽取 `_tweenSize` / `_forEachTextLabel`。改为**通过**（🟡=0）。§1.8 残留：既有 ToolSystem 规范修复，无新 Remote/存档/§1.6/§1.7；`IsClient` 仅客户端 UI 守卫。未改历史 API 名（`showTips`/`NormalTips`）与 `UIStorke` 拼写。 |
| 2026-09-01 | RayCast 审查并修 P0/P1/P2（随审 `RayCast-Type`）：无 §1.6/§1.7。类型档补五项头与表名 RayCast；Author 对齐林奥宇；区块顺序；`Log.warn`；文件夹缓存；配置/instances 拷贝 + freeze；`maxDistance` 守卫；透明穿透 Exclude 加入过滤。改为**通过**（🟡=0）。§1.8 残留：单文件规范与热路径缓存，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | ShowDetail 审查并修 P0/P1/P2（随审 `ShowDetail-Type`）：无 §1.6/§1.7。GamePass `ZhDes` 改 `TranslationHelper.SetText`；去掉 Class.Struct 长路径 require；Author 对齐林奥宇；私有 `_` 前缀；定位改 if-elseif + 无 WaitForChild；抽取 `_getDetailScreen` / `_invokeDetailScript`；pcall 失败 `Log.warn`。改为**通过**（🟡=1：`EnumMgr.ItemType.Hero` 未注册，英雄跳过恒为假）。§1.8 残留：单文件规范与本地化，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | RequestRateLimit 审查并修 P0/P1/P2（随审 `RequestRateLimit-Type`）：无 §1.6/§1.7。ErrorTips Key 改字面量并入 LocalizationTable；Author 对齐林奥宇；`NetMsg` 常量；配置深拷贝；写接口服务端门控与入参守卫。改为**通过**（🟡=0）。§1.8 残留：既有中间件合规范，无新 Remote/存档字段；Kick 文案仍用常量（服 TranslateByKey 非玩家 Locale）。 |
| 2026-09-01 | UIanima 审查并修 P0/P1/P2（随审 `UIanima-Type`、Helpers / ItemFly / NumberChange）：飞入数量改 SetRawText；补 AnimateNumberChange（TopBarManager 已依赖）；Author 对齐林奥宇；资源走 AssetPaths；拆目录模块（init 263 / Helpers 135 / ItemFly 117 / NumberChange 118 有效行）。改为**通过**（🟡=0）。§1.8 残留：展示动画与规范修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | UIMgr 审查并修 P0/P1/P2（随审 `UIMgr-Type` / LengthAdjustment）：稀有度名改字面量 SetText 并入 LocalizationTable；类型档补五项头与渐变 API 注释；`cfg` 字段形；服务走 Utils；拆 `SetUIlistSize_New`；守卫 FindFirstChild / FindCfgByID；`GradientCfg` 表名；closeUi `Log.warn`。改为**通过**（🟡=0）。§1.8 残留：既有 ToolSystem 规范与本地化，无新 Remote/存档/§1.6/§1.7。未改历史 API 名（`SetTitel` / `getXydName` / `GetRaceByxyd`）。 |
| 2026-09-01 | PartIcles 审查并修 P0/P1/P2（随审 TypeRegistry、FXUtil.PartIcles 门面）：无 §1.6/§1.7。45 文件补五项头（Author 林奥宇）；服务走 Utils；`warn`→`Log.warn`；init/TypeData require 移顶；公共/私有块注释与 `@param`/`@return` 语义。改为**有条件通过**（🟡=4：Events/Emit/Update/TypeData 有效行＞500；约 25 个函数＞80 行；部分私有无 `_` 前缀；ActiveEmits 等引擎表仍挂公共面）。§1.8 残留：热路径不拆；`IsClient` 仅焦点/PreRender/PlayerGui 能力门控。无新 Remote/存档。 |
| 2026-09-01 | PurchaseManager 审查并修 P0/P1/P2：无 §1.6/§1.7。MarketplaceService 走 Utils；Init/收据 pcall；passID 与 ProcessReceipt 返回值守卫；决策枚举常量化。改为**通过**（🟡=0）。§1.8 残留：既有 Manager 编排修复，无新 Remote/存档字段。 |
| 2026-09-01 | LogicalDisplace 复审并修 P0/P1/P2（随审 LogicalDisplaceProfile）：初审 IsServer / `@param` 红项代码侧已闭合。Players 走 Utils；`_armMoveLocks` 移回私有区；`MAX_DT` / 缓动默认名 / `FALLBACK_LOOK`；`tryApplyFromHitbox` 返回 boolean；未知档名 `Log.warn`；Profile 抽 `_copy` 并补分区。改为**通过**（🟡=0）。§1.8 残留：既有击退规范修复，写口仍只经 `EnemyLogicalStore.setCFrame` + EnemySim 互斥；无新 Remote/存档/§1.6。 |
| 2026-09-01 | BgmManager 审查并修 P0/P1/P2：无 §1.6/§1.7。先建后停避免失败静音；创建失败不锁名且同曲只 warn 一次；stopSound 保留实例；文件头补加载方式；CharacterAdded 立即刷新。改为**通过**（🟡=0）。§1.8 残留：LocalScript 编排修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | UtilsSystem 复审并修 P0/P1/P2（随审 ModuleRegistry）：初审文件头/私有注释已闭合。表名改 UtilsSystem；Author 对齐林奥宇；拆目录（init 402 / ModuleRegistry 403 有效行）；服务顶部缓存；循环 require 改 pcall；Folder+init 统一解析；成功加载 rawset 缓存。改为**通过**（🟡=0）。§1.8 残留：基础设施规范修复，无新 Remote/存档/§1.6/§1.7；`IS_SERVER` 仅模块加载分流。 |
| 2026-09-01 | Meteor1 复审并修 P0/P1/P2：Author 对齐林奥宇；抽出 `_scheduleWandTrail`/`_scheduleMagicCircle`/`_cleanupMeteorTrail`；`onEnd` 打断兜底关尾迹；Recovery 可 Interrupt；Hitbox CollisionGroup 改为 Hitbox；剩余魔法串常量化。改为**通过**（🟡=0）。§1.8 残留：既有 SkillModule 规范修复；打断补清尾迹与 Recovery 可打断不改伤害/位姿写口。 |
| 2026-09-01 | NewRoll 复审：上次 P0（组技能 Utils 链 / 五项头 / `any` 字段）与 P1（声明式 condition、guard、onEnd 断烟尘）已闭合。无 §1.6/§1.7。改为**通过**（🟡=1：Rolling 状态钟 0.4s 与侧向电机 0.45s 差约 50ms，已注释待策划确认）。§1.8 残留：既有 SkillModule 规范修复；`IsServer` 仅加载 `SystemCharacterMotion`；未改手感钟。 |
| 2026-09-01 | 有条件通过队列复审（PartIcles / Animate / SystemMount）：Animate 改为**通过**（🟡=2：GroundLocomotion/AnimateModule 超 500 有效行不宜拆；ClimbAssist 未走 Utils.Workspace）。PartIcles **维持有条件通过**（🟡=4：Events/Emit/Update/TypeData 超 500；超长函数；私有无 `_`；ActiveEmits 挂公共面）。SystemMount 客/服 **维持有条件通过**（黄项＞3：三份 Driver/表现超 500；HasMount 未接 SystemSave；RequestExit 缺注释；客 Init 无 Unbind）。无新增 §1.6/§1.7 红牌。 |
| 2026-09-01 | PartIcles / SystemMount 黄项已修：引擎表改 `_activeEmits` 等且门面不透出；私有 local function 补 `_`；客 `RequestExit` 注释 + `destroy`/`Unbind`；服 TryExit 用 `reason`；Presentation 走 Utils。改为**通过**（PartIcles 🟡=2：热路径大文件/超长函数不拆；SystemMount 🟡=2：Driver/表现超 500 不宜拆；HasMount 接 SystemSave 须另开方案）。无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | B1 有条件通过四项黄项已修：LogicEntityMotion 拆 `integrate` / 分区 / `CHASE_RUN_MUL`；LogicalPathFollow 与 Command 共用 `EnumMgr.LogicalDriveMode`；EnemyLogicalStore 头与活引用约定；SystemLogicalEnemy 注释字段形 + `Utils.RunService`。改为**通过**（SystemLogicalEnemy 🟡=1：Follow/Displace/Spawn 热路径不拆）。无新 Remote/存档/§1.6/§1.7；位姿仍只经 `setCFrame`。 |
| 2026-09-01 | SystemInput / ActionArbiter / SystemCode 黄项已修：ContextStack 查询拷贝；ActionArbiter `Create` 拆 `GateInstance`、OnChanged 快照拷贝、Bridge `_` 前缀；SystemCode `CodeErr` + RegisterNetWork 幂等。改为**通过**（SystemInput 🟡=1：Jump 未收敛；SystemCode 🟡=1：普通码仍 TODO）。无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | SystemRank 复审并修 P0/P1/P2（随审 SystemRank-Type）：无 §1.6/§1.7。`@return` 补 `-` 语义；Author 对齐林奥宇；文件头补编码/时长榜/镜像约定；时长榜读旧分失败不写；`3`/`200`/`1` 抽常量；前三名改 InsMgr。改为**通过**（🟡=0）。§1.8 残留：既有写榜规范修复；时长榜 fail-closed 不改写口归属。无新 Remote/存档字段。 |
| 2026-09-01 | LocoStyle 复审并修 P0/P1/P2（随审 StyleInstance / LocoStyle-Type）：无 §1.6/§1.7。类型桩补五项头与 `@return` 语义；`Create` 拆 `StyleInstance`（state + 模块级函数）；`SLOT_BIND_META` freeze；CreateDeps 字段形；分区 / NaN 注释。改为**通过**（🟡=0）。§1.8 残留：既有 ToolSystem 同行为拆分，无新 Remote/存档/写口。 |
| 2026-09-01 | SystemCharacterMotion 复审并修 P0/P1/P2：服务走 Utils；`warn`→`Log.warn`；`Init`/`destroy`；无窗断开 Heartbeat；开窗缓存 HRP；魔法数/kind 常量化；修改类返回 boolean；UpdateManager 接线 Init。改为**通过**（🟡=0）。§1.8 残留：既有包络规范修复，无新 Remote/存档/§1.6；击退仍只经本模块 Fire* → CharacterMotion；能力默认 tween 仍 0.7。 |
| 2026-09-01 | DailyReward 复审并修 P0/P1/P2：Author 对齐林奥宇；`updateUi` 缺签到字段回退 PlayerData（`{ day = 1 }` 不再覆盖 LastDailyTime / 比较报错）；数量与倒计时改 SetRawText；抽出私有函数+分区；可见时才跑倒计时；`AddMouseClick`；领取互斥；`destroy`。改为**通过**（🟡=0）。§1.8 残留：GuiScripts 展示与规范修复，无新 Remote/存档/§1.6/§1.7。宠物视口仍不填模型（与初审一致，须确认 ResourceUtil 分类）。 |
| 2026-09-01 | RewardWindow 复审并修 P0/P1/P2：Author 对齐林奥宇；`heroIDVt` 补本批首次标记；`ItemType.Hero` 改 `Pet`；GlobalCfg pcall+超时+常量；拆私有函数+分区；`AddMouseClick`；Debris 清格。改为**通过**（🟡=1：`抽取获得碎片` 仍直 require GlobalCfg，未进 GameConfig）。§1.8 残留：GuiScripts 展示修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | SkillHitPresentation 复审并修机械 P1/P2：表名改 SkillHitPresentation；stub/公共 `@param` 字段形；Config/SoundMap freeze；`warn`→`Log.warn`；魔法串常量化；Utils.Players；抽出 `_addUniqueSound`。`IsServer` 未拆（须 §1.8）。改为**不通过**。 |
| 2026-09-01 | SkillHitPresentation 拆层方案：服 System + 客 ToolSystem stub，对照 NPCCosmeticHit。§1.8 **有条件通过**。未开工。 |
| 2026-09-01 | SkillHitPresentation §1.8 有条件通过后开工：写口迁 ServerSideCode；ToolSystem 去 IsServer；删 Resolver；Utils `_preferServerSystemModule`。 |
| 2026-09-01 | SkillHitPresentation 拆层复审：架构 / IsServer / §1.6 / §1.7 闭合。改为**通过**（服/客 🟡=2：SoundMap 头仍写 Resolver；accumulateFromLogical 公共参 `_logicalId`）。§1.8 残留：服容器 require Profile/SoundMap，不扩 Utils 键。无新 Remote/存档。 |
| 2026-09-01 | SkillHitPresentation 黄项已修：SoundMap 头改为服 System 容器 require；`accumulateFromLogical` 公共参改 `logicalId`。改为**通过**（🟡=0）。§1.8 残留不变。 |
| 2026-09-01 | SystemUpdateLog 复审并修 P0/P1/P2：Author 对齐林奥宇；去掉模块加载 `IsServer`；RegisterNetWork 幂等+RequestRateLimit；`ChangePlrData` 检查返回值并深拷贝后整表回写；`EnumMgr.UpdateLogErr`；Remote 失败 Log；`IsA`/`IS_READ`/`CFG_NAME`。改为**通过**（🟡=0）。§1.8 残留：既有已读写口规范修复，无新 Remote/存档字段/§1.6/§1.7。 |
| 2026-09-01 | DebugSystem 复审并修 P0/P1/P2（随审 DebugPanel / DebugI18n）：动态按钮 `SetText` 对 TextButton 无效且 Key 走变量，改为 Caption 子 Label + 分支字面量；拆目录；去模块级 spawn，改由 DebugManager 调 CreateUI；悬停/常量抽取。改为**通过**（🟡=0）。§1.8 残留：GuiScripts 规范修复，无新 Remote/存档/§1.6/§1.7；新动态按钮须在 DebugI18n 补字面量分支。 |
| 2026-09-01 | SystemRankManager 复审并修 P0/P1/P2（随审 RankManager / SystemRankManager-Type）：去掉客户端写 `前3名玩家`；刷新改客户端 Heartbeat（不再误用仅服务端 UpdateModule）；可见文案 SetRawText；Init 收到 RankManager；格子引用缓存；`@return` 语义；Author 对齐林奥宇。改为**通过**（🟡=0）。§1.8 残留：既有展示规范修复；写口只留 SystemRank；无新 Remote/存档/§1.6。 |
| 2026-09-01 | UpdateManager 复审并修 P0/P1/P2：Author 对齐林奥宇；服务走 Utils；JSONDecode/GetAsync pcall；抽出 `_clearReservedServerRecord` / `_notifyAndTeleportOnShutdown` / `_onServerTimeTick` / `_onBindToClose` / `_init`；Studio UpdateRank 挪到 Init 后；地理查询改有界 for（去掉 while true 与 11 次偏多）；`@return` 补 `-` 语义；魔法串常量化。改为**通过**（🟡=0）。§1.8 残留：既有 Manager 编排修复；关服清 Global 仍走 SystemDataStore（既有键，不下沉新 System）；无新 Remote/存档字段/§1.6/§1.7。 |
| 2026-09-01 | ModelFind 复审：源文件 `ToolSystem/ModelFind.luau` 与 `ModelFind-Type` 已于 2026-07-02（`58d1ead`）删除，职责迁入 `AssetAcquire` / `ResourceUtil`。`src/` 无残留引用。无代码可修。从 §4 有条件通过摘除，改随审 AssetAcquire / ResourceUtil。 |
| 2026-09-01 | AssetAcquire 复审并修 P0/P1/P2（随审 `AssetAcquire-Type`）：Author 对齐林奥宇；类型档补五项头/表名/API 注释；GetModel 先校验 Model 再 Clone/借池；Clone pcall；抽出 `_acquireFromTemplate`。改为**通过**（🟡=0）。§1.8 残留：单文件规范与泄漏收口，无新 Remote/存档/§1.6/§1.7；clone=false 仍返回模板活引用。 |
| 2026-09-01 | FootStepManager 复审并修 P0/P1/P2（随审 FootstepSoundModule）：Author 对齐林奥宇；内部方法改私有；服务走 Utils；`destroy` 销毁在播 Sound 并断 Ended；Manager 只编排；WaitForChild 超时；射线改 `RayCast.GetRayCastParams`；池创建抽取。改为**通过**（🟡=0）。§1.8 残留：既有客户端脚步编排规范修复，无新 Remote/存档/§1.6/§1.7。 |
| 2026-09-01 | ClockManager 复审并修 P0/P1/P2（服 `ClockManager.server` + 客 `ClockManager.client`）：Author 对齐林奥宇；`any` 表字段形；Utils 顶部缓存；魔法数/段名/Instance 名常量化；去掉占位 Night 先写；段名字典序匹配；配置非法 Log；抽出客私有回调。改为**通过**（🟡=0）。§1.8 残留：时钟规则仍留本 Manager（Lighting 文档指定的权威发布者）；下沉 SystemClock 须另开方案。无新 Remote/存档/§1.6；ClockAuthority 仍只由服端本脚本写。 |
| 2026-09-01 | CfgFind 复审并修 P0/P1/P2（随审 CfgFind-Type）：查询出口改回 `Copy.deepCopy`（`_copyCfg` 避开 `deepCopy(nil)→{}`）；Author 对齐林奥宇；区块顺序；私有 `_` 前缀；兜底扫描补 gamepass/mount；主键 number/字符串互通；`maxWeekID` 类型守卫。改为**通过**（🟡=0）。§1.8 残留：既有 ToolSystem 规范修复，与 2026-07-02 初审契约一致；freeze 仍由 ConfigPrepare 保护内存表；无新 Remote/存档/§1.6/§1.7。未改历史 API 名（`GetNowVisionCfg` / `FindCfgByPassID_Gift`）。 |
| 2026-09-01 | TimeTransfer 复审并修 P0/P1/P2（随审 `TimeTransfer-Type`）：Author 对齐林奥宇；文件头补模块关系/示例；类型档补 `FormatUnixTimestampToYMD`；抽取时长拆分；Unix 阈值常量；`FormatTimeS` 空格对齐注释。改为**通过**（🟡=0）。§1.8 残留：单文件展示格式化，无新 Remote/存档/§1.6/§1.7；`UniqueTimeString` 英文单位与 `UniqueTimeString_Rank` 别名保留。 |
| 2026-09-01 | AFKManager（客）复审并修 P0/P1/P2：Author 对齐林奥宇；`_getConfigNumber` 补块注释与 `@param`/`@return`；区块顺序（常量→私有变量→私有函数→初始化）；文件头补 LocalScript 约定与配置键；配置非法 `Log.warn`；路径 freeze；idleTime 类型守卫。改为**通过**（🟡=0）。§1.8 残留：LocalScript 规范修复，无新 Remote/存档/§1.6/§1.7；客仍仅上报，传送权威在服。 |
| 2026-09-01 | ResourceUtil 复审并修 P0/P1/P2（随审 `ResourceUtil-Type`）：Author 对齐林奥宇；文件头补 Module/关系/Scope/Options/示例；类型档五项头+表名+Mount/AllGridients；`BackToPool` 透传 `true`/`false`+`ObjectPoolUtil.Err`。改为**通过**（🟡=0）。§1.8 残留：既有门面规范修复，无新 Remote/存档/§1.6/§1.7；枚举仍为 freeze 源表同一引用。 |
| 2026-09-01 | HeadTrack 复审并修 P0/P1/P2：Author 对齐林奥宇；去掉 TopbarPlus Spring2 长路径，改本机 Neck.C0 一阶平滑；Heartbeat 缓存 HRP/Neck；`destroy` 断连接；Neck `IsA("Motor6D")`；配置进 GameConfig；超时 `Log.warn`。改为**通过**（🟡=0）。§1.8 残留：LocalScript 表现逻辑仍留 Manager（不下沉新 System）；无新 Remote/存档/§1.6；Neck.C0 仅本机表现，Animator 仍写 Transform。 |
| 2026-09-01 | VisibleMgr 复审并修 P0/P1/P2（随审 `VisibleMgr-Type`）：Author 对齐林奥宇；服务走 Utils；SpeedDown/SpeedDownY 拒绝玩家角色子树；文件头使用说明；抽取 `_forEachBasePart` / `_applyFadeNode` / 角色渐隐私有函数；粒子循环防叠；fadeAllTween 与 fadeAll 关闭同一组表现类；Weld 先设 Part 再 Parent。改为**通过**（🟡=1：有效行仍超 500，不宜本轮拆目录）。§1.8 残留：单文件规范与速度写口收紧，无新 Remote/存档/§1.6；玩家速度仍只经 CharacterMotion；未改历史 API 名（`fadeAll` / `UnTransparencyAll`）。 |
| 2026-09-01 | UIManager 复审并修 P0/P1/P2：Author 对齐林奥宇；ReplicatedStorage 走 Utils；排队改为 `QUEUEABLE_UI_NAMES` 常量+FIFO；无动画关闭真正隐藏并刷新模糊；Frame/模块缓存；Remote `uiName`/`isShow` 守卫；拆 `_hideUi`/`_openUi`/`_refreshUi`。改为**通过**（🟡=0）。§1.8 残留：既有 LocalScript 编排修复，无新 Remote/存档/§1.6/§1.7；GuiScripts 仍按名惰性 require；排队名单默认空。 |
| 2026-09-01 | HatchEffect 复审并修 P0/P1/P2（随审 HatchEffect-Type）：`@param`/`@return` 补语义；类型档五项头；`warn`→`Log.warn`；CurrentCamera 每次读取；缩放保存/还原；clamp 恒拷贝；跳过输入主线程收口结束动画；重叠调用互斥；GetModel/PrimaryPart 空守卫；魔法数进 HatchConfig；ShowInfoUI camelCase。改为**通过**（🟡=0）。§1.8 残留：既有表现规范修复，无新 Remote/存档/§1.6；摄像机仍只写 CameraMinZoomDistance。`EnumMgr.Rare.Xyd6` 仍未注册（原分支保留）。 |
| 2026-09-01 | SkillPreloadUtil 复审并修 P0/P1/P2：Author 对齐林奥宇；`any` 改为 `BaseSkillPreloadConfig`；服务走 Utils；抽出 `_safeRequireModule` / `_collectBaseSkillLists` / `_preloadSoundsByNames`；入参守卫；缺资源 Log.warn；pcall 检查返回值；基础技能去重；音频未命中重建一次索引。改为**通过**（🟡=0）。§1.8 残留：既有 ToolSystem 预热编排规范修复，无新 Remote/存档/§1.6/§1.7；技能配置仍按名懒加载 ModuleScript。 |
| 2026-09-01 | SequenceManager 复审并修 P0/P1/P2（随审 CatalogImages / SequenceManager-Type）：Author 对齐林奥宇；服务走 Utils；PreLoad 补块注释；Catalog 拆 CatalogImages（有效行 507→380）；PlaySequence/Preload/Contract 拆私有函数；类型桩表名 SequenceManager；PeerPool Acquire 失败 Log；叠化除零与 settle 拍数常量化。改为**通过**（🟡=0）。§1.8 残留：既有 ToolSystem 规范修复，无新 Remote/存档/§1.6/§1.7；`IsClient` 仅客户端播放/复制等待门控；Shutdown 仍须调用方在离场调用。 |
| 2026-09-01 | B1 表已通过但未写入 §4 通过列的 11 项补入索引（结论不变）：LogicalMoveCommand、PathFindUtil、EnemySim、RangerAi、SystemEnemy（服 / AllSide stub）、EnemyLogicalSync、EnemyLogicalTypes、LogicalAirborne、LogicalAirborneProfile、EnemyManager。 |
| 2026-09-01 | CharacterControl 复审并修 P0/P1/P2（随审 ControlInstance / CharacterControl-Type）：Author 对齐林奥宇；Create 拆 ControlInstance；服务走 Utils；去掉 `_sharedHolderCount` 私有导出；Bridge 改 Acquire/Release/ReleaseAll；LockMask OnChanged 快照拷贝；角色离树 Heartbeat 自 destroy；类型桩表名与 CreateOptions 字段形；魔法数进 Constants。改为**通过**（🟡=0）。§1.8 残留：既有 Control 规范修复与同行为拆分，无新 Remote/存档/§1.6；WalkSpeed 仍只经 CMS Modifier；Jump 仍只经 JumpPolicy。 |
| 2026-09-01 | TranslationHelper 复审并修 P0/P1/P2（随审 LanguageCfg / TranslationHelper-Type）：Author 对齐林奥宇；文件头使用说明；类型档五项头与表名；SetRawText 写 ProximityPrompt 并解绑 `代码本地化`；`warn`→`Log.warn`；gsub `%` 转义；HasKey / 空串守卫。改为**通过**（🟡=0）。§1.8 残留：既有 ToolBasic 规范修复，无新 Remote/存档/§1.6/§1.7；未改 `Language.luau`。 |
| 2026-09-01 | Feedback 审查并修 P0/P1/P2（随审 SystemFeedback / SystemFeedback-Type）：Tips Key 改调用处字面量并入 LocalizationTable；Author 对齐林奥宇；去掉 `IsServer`；RegisterNetWork 幂等+RequestRateLimit；Webhook 送达后写档失败仍 true；去空白后再计字数；类型档五项头。改为**通过**（🟡=0）。§1.8 残留：既有反馈 UI/Remote 规范修复，无新 Remote/存档字段/§1.6/§1.7。 |
| 2026-09-01 | 排行榜复审黄/绿项已修（SystemRankManager 目录 + RankConfig）：子模块文件头补职责边界；内嵌 helper 块注释；删无调用方 `waitForLobbyRankFolder`/`waitForMappedBoard`；Bind/Refresh 拆 `_bindSingleBoard`/`_applyRankBoardEntries`；标题仍字面量分支（本地化禁止 KEYS 表）；GetRankDef 注释标明冻结只读引用；名单路径改目录版。维持**通过**（🟡=0）。§1.8 残留：注释/死 API/同行为拆分，无新 Remote/存档/§1.6。 |
| 2026-09-01 | SystemRankManager P1：`connectSceneRankWatches` 内嵌 watch 提为 `_watchRankFolder`/`_tryWatchFeature`/`_watchWorld`（共享 `SceneRankWatchState`）。维持**通过**（🟡=0）。 |
